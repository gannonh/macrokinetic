---
phase: 30-dashboard-foundation
plan: 01
type: execute
---

<objective>
Create the widget protocol and container components for the new dashboard.

Purpose: Establish the foundational abstractions (DashboardWidget protocol, WidgetCard, HeroWidgetContainer, StandardWidgetGroup) that all dashboard widgets will use.
Output: 4 new Swift files providing reusable widget infrastructure.
</objective>

<execution_context>
~/.claude/gsd/workflows/execute-phase.md
~/.claude/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Design Reference:**
@mocks/dashboard/DASHBOARD.png
@mocks/dashboard/Hero Widget.png
@mocks/dashboard/Standard Widget, Grouped.png

**Existing patterns to follow:**
@JabTracker/Design/CardComponents.swift
@JabTracker/Design/DesignTokens.swift
@JabTracker/Onboarding/Views/USPShowcaseStepView.swift

**Prior decisions affecting this phase:**
- Clean Architecture approach selected - protocol-based widget system with separate files
- Static mockup data first, wire to live data in Phase 34
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create widget protocol and card wrapper</name>
  <files>
    JabTracker/Views/Dashboard/Widgets/DashboardWidget.swift
    JabTracker/Views/Dashboard/Widgets/WidgetCard.swift
  </files>
  <action>
    **DashboardWidget.swift (~40 lines):**
    Create protocol defining contract for all dashboard widgets:
    ```swift
    protocol DashboardWidget: Identifiable {
        associatedtype Content: View
        var id: String { get }
        var title: String { get }
        @ViewBuilder var content: Content { get }
    }
    ```

    **WidgetCard.swift (~50 lines):**
    Create card wrapper component that:
    - Wraps content in DesignCard-style container (use `.cardStyle()` modifier)
    - Adds optional tap gesture for navigation
    - Includes chevron.right icon when tappable
    - Has `.accessibilityIdentifier()` for testing

    Pattern to follow: Similar to DesignCard but with optional onTap action.
    Use `DesignTokens.Colors.cardBackground` and `DesignTokens.CornerRadius.card`.
  </action>
  <verify>Build succeeds, SwiftLint passes, files appear in Xcode after xcodegen</verify>
  <done>DashboardWidget protocol and WidgetCard wrapper created with proper styling</done>
</task>

<task type="auto">
  <name>Task 2: Create HeroWidgetContainer and StandardWidgetGroup</name>
  <files>
    JabTracker/Views/Dashboard/Widgets/HeroWidgetContainer.swift
    JabTracker/Views/Dashboard/Widgets/StandardWidgetGroup.swift
  </files>
  <action>
    **HeroWidgetContainer.swift (~100 lines):**
    Create swipeable carousel container:
    - Uses `TabView` with `.tabViewStyle(.page(indexDisplayMode: .never))`
    - `@State private var selectedIndex = 0` for page tracking
    - Custom page indicator dots below carousel (8pt circles, 8pt spacing)
    - Selected dot: `Color.white`, Unselected: `Color.white.opacity(0.3)`
    - Wraps entire thing in DesignCard-style container
    - Frame height ~400pt to accommodate hero content
    - Animation: `.easeInOut` on selectedIndex change

    Follow pattern from USPShowcaseStepView.swift lines 47-69.

    **StandardWidgetGroup.swift (~80 lines):**
    Create 2x2 grid container:
    - Section header: "Insights & Analytics" using `DesignTokens.Typography.headline`
    - Uses `LazyVGrid(columns: [GridItem(.flexible()), GridItem(.flexible())], spacing: 12)`
    - Each cell contains a widget
    - 12pt spacing between widgets
    - No outer card - widgets provide their own card styling

    Both containers should accept generic widget content via ViewBuilder or array of views.
    Add `.accessibilityIdentifier()` to both containers.
  </action>
  <verify>Build succeeds, SwiftLint passes, preview renders for both containers</verify>
  <done>HeroWidgetContainer carousel and StandardWidgetGroup grid created with proper layout</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodegen generate` succeeds
- [ ] `./scripts/build.sh` succeeds without errors
- [ ] `swiftlint` passes with no new violations
- [ ] All 4 files exist in JabTracker/Views/Dashboard/Widgets/
- [ ] Preview renders for WidgetCard, HeroWidgetContainer, StandardWidgetGroup
</verification>

<success_criteria>
- DashboardWidget protocol defines clear contract for widgets
- WidgetCard provides consistent card styling with optional tap
- HeroWidgetContainer provides swipeable carousel with page dots
- StandardWidgetGroup provides 2x2 grid layout with header
- All components follow existing DesignTokens patterns
- All components have accessibility identifiers
</success_criteria>

<output>
After completion, create `.planning/phases/30-dashboard-foundation/30-01-SUMMARY.md`:

# Phase 30 Plan 01: Foundation & Containers Summary

**[One-liner describing what shipped]**

## Accomplishments
- [Key outcomes]

## Files Created
- `JabTracker/Views/Dashboard/Widgets/DashboardWidget.swift` - Widget protocol
- `JabTracker/Views/Dashboard/Widgets/WidgetCard.swift` - Card wrapper
- `JabTracker/Views/Dashboard/Widgets/HeroWidgetContainer.swift` - Carousel container
- `JabTracker/Views/Dashboard/Widgets/StandardWidgetGroup.swift` - Grid container

## Decisions Made
[Any decisions made during implementation]

## Issues Encountered
[Problems and resolutions, or "None"]

## Next Step
Ready for 30-02-PLAN.md (Hero Widgets)
</output>
