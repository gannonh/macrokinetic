---
phase: 34-integration-polish
plan: 1
type: execute
---

#### Objective

Wire Hero Widgets to live data sources (MealLogService, TDEEService, User goals).

Purpose: Replace mock data in 3 hero carousel widgets with real nutrition/energy data
Output: Hero widgets display actual user data with real-time updates

#### Execution Context

./execute-phase.md
~/.claude/gsd/templates/summary.md

#### Project Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md

**Prior phase summaries:**
@.planning/phases/31-main-widget-hero/31-02-SUMMARY.md

**Key service files:**
@JabTracker/Services/MealLogService.swift
@JabTracker/Services/TDEEService.swift

**Widget files to modify:**
@JabTracker/Views/Dashboard/Widgets/WeeklyNutritionHeroWidget.swift
@JabTracker/Views/Dashboard/Widgets/DailyNutritionHeroWidget.swift
@JabTracker/Views/Dashboard/Widgets/EnergyBalanceHeroWidget.swift

**Existing services provide:**
- `MealLogService.getDailyTotals(for: Date)` → DailyNutritionTotals (calories, protein, carbs, fat, fiber)
- `TDEEService` → TDEE calculations and estimates
- `User.nutritionGoals` → daily calorie/macro targets

#### Tasks

<task type="auto" approach="tdd">
  <name>Wire DailyNutritionHeroWidget to live data</name>
  <files>
    JabTracker/Views/Dashboard/Widgets/DailyNutritionHeroWidget.swift
    JabTracker/ViewModels/DailyNutritionHeroViewModel.swift (create)
  </files>
  <action>
    Create DailyNutritionHeroViewModel that:
    1. Injects MealLogService and fetches today's DailyNutritionTotals
    2. Gets User's nutritionGoals for targets
    3. Exposes consumed/target values for calories, protein, carbs, fat
    4. Adds @Published loading state and error handling
    5. Keeps mock data as .preview static for SwiftUI previews

    Update DailyNutritionHeroWidget to use ViewModel instead of DailyMockData.
    Maintain existing UI structure - only change data source.
  </action>
  <verify>
    - Unit tests for ViewModel data fetching
    - Widget displays real data when service has entries
    - Widget handles empty state gracefully
    - Preview still works with mock data
  </verify>
  <done>DailyNutritionHeroWidget shows actual today's nutrition vs user targets</done>
</task>

<task type="auto" approach="tdd">
  <name>Wire WeeklyNutritionHeroWidget to live data</name>
  <files>
    JabTracker/Views/Dashboard/Widgets/WeeklyNutritionHeroWidget.swift
    JabTracker/ViewModels/WeeklyNutritionHeroViewModel.swift (create)
  </files>
  <action>
    Create WeeklyNutritionHeroViewModel that:
    1. Fetches 7 days of DailyNutritionTotals via MealLogService
    2. Gets User's nutritionGoals for daily targets
    3. Calculates fill percentages for each day's macro bars
    4. Today = partial (current progress), future = 0%, past = actual
    5. Exposes arrays for calories[], protein[], carbs[], fat[] + targets

    Update WeeklyNutritionHeroWidget to use ViewModel.
    Keep WeeklyMockData as .preview static for SwiftUI previews.
  </action>
  <verify>
    - Unit tests for 7-day data aggregation
    - Widget shows correct fill levels for past days
    - Today shows current progress
    - Future days show 0%
  </verify>
  <done>WeeklyNutritionHeroWidget displays actual 7-day nutrition history</done>
</task>

<task type="auto" approach="tdd">
  <name>Wire EnergyBalanceHeroWidget to live data</name>
  <files>
    JabTracker/Views/Dashboard/Widgets/EnergyBalanceHeroWidget.swift
    JabTracker/ViewModels/EnergyBalanceHeroViewModel.swift (create)
  </files>
  <action>
    Create EnergyBalanceHeroViewModel that:
    1. Fetches 30 days of daily calories via MealLogService
    2. Fetches 30 days of TDEE estimates via TDEEService (or uses current TDEE × 30)
    3. Gets User's nutritionGoals for calorie targets
    4. Calculates daily balance: consumed - expenditure (for expenditure mode)
    5. Calculates daily balance: consumed - target (for targets mode)
    6. Exposes dailyData array, averageExpenditure, averageTargets
    7. Responds to @Environment(\.energyDisplayMode) for mode toggle

    Update EnergyBalanceHeroWidget to use ViewModel.
    Keep EnergyBalanceMockData as .preview static.
  </action>
  <verify>
    - Unit tests for balance calculations
    - Widget shows correct chart with real data
    - Mode toggle switches data source correctly
    - Handles missing TDEE data gracefully
  </verify>
  <done>EnergyBalanceHeroWidget displays actual 30-day energy balance data</done>
</task>

#### Verification

- [ ] All 3 hero widgets compile and display real data
- [ ] SwiftUI previews still work with mock data
- [ ] ViewModels have unit test coverage
- [ ] No regressions in widget visual appearance
- [ ] Build succeeds, SwiftLint passes

#### Success Criteria

- Hero carousel shows actual user nutrition data
- Empty states handled (new users with no data)
- Mode toggles work correctly with live data
- Performance acceptable (no UI lag during data fetch)

#### Output

After completion, create `.planning/phases/34-integration-polish/34-01-SUMMARY.md`
