---
phase: 34-integration-polish
plan: 3
type: execute
---

#### Objective

Wire Detail Views to live data sources for full historical analysis.

Purpose: Replace mock data in 3 detail views with real historical data
Output: Detail views display actual user history with time period filtering

#### Execution Context

./execute-phase.md
~/.claude/gsd/templates/summary.md

#### Project Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md

**Prior plan summaries:**
@.planning/phases/34-integration-polish/34-01-SUMMARY.md
@.planning/phases/34-integration-polish/34-02-SUMMARY.md

**Key service files:**
@JabTracker/Services/MetricsService.swift
@JabTracker/Services/TDEEService.swift
@JabTracker/Services/MealLogService.swift

**Detail view files to modify:**
@JabTracker/Views/Dashboard/DetailViews/WeightTrendDetailView.swift
@JabTracker/Views/Dashboard/DetailViews/ExpenditureDetailView.swift
@JabTracker/Views/Dashboard/DetailViews/EnergyBalanceDetailView.swift

**Shared component:**
@JabTracker/Views/Dashboard/DetailViews/DetailTimePeriodSelector.swift

#### Tasks

<task type="auto" approach="tdd">
  <name>Wire WeightTrendDetailView to live data</name>
  <files>
    JabTracker/Views/Dashboard/DetailViews/WeightTrendDetailView.swift
    JabTracker/ViewModels/WeightTrendDetailViewModel.swift (create)
  </files>
  <action>
    Create WeightTrendDetailViewModel that:
    1. Queries MetricsService.getWeightEntries() for selected time period
    2. Calculates trend weight via exponential moving average
    3. Computes weight changes at 3/7/14/30/90 day intervals
    4. Estimates energy balance from weight change rate
    5. Projects 30-day weight based on current trend
    6. Responds to DetailTimePeriod selection changes

    Update WeightTrendDetailView to use ViewModel.
    Keep WeightTrendDetailData.mock for previews.
  </action>
  <verify>
    - Unit tests for trend calculations and projections
    - Chart displays correctly with real data
    - Time period filter works
    - Handles sparse data (missing days)
  </verify>
  <done>WeightTrendDetailView shows actual weight history with trend analysis</done>
</task>

<task type="auto" approach="tdd">
  <name>Wire ExpenditureDetailView to live data</name>
  <files>
    JabTracker/Views/Dashboard/DetailViews/ExpenditureDetailView.swift
    JabTracker/ViewModels/ExpenditureDetailViewModel.swift (create)
  </files>
  <action>
    Create ExpenditureDetailViewModel that:
    1. Gets TDEE history from TDEEService (or current TDEE for all dates)
    2. Gets data quality/confidence from TDEECalculationEngine
    3. Calculates expenditure changes at different intervals
    4. Maps quality tier to status (fluxRange/updating/holding)
    5. Responds to DetailTimePeriod selection

    Update ExpenditureDetailView to use ViewModel.
    Keep ExpenditureDetailData.mock for previews.

    Note: TDEE typically doesn't vary daily - may show same value across time.
  </action>
  <verify>
    - Unit tests for expenditure data retrieval
    - Chart displays correctly
    - Status indicator reflects data quality
    - Time period filter works
  </verify>
  <done>ExpenditureDetailView shows TDEE history and data quality</done>
</task>

<task type="auto" approach="tdd">
  <name>Wire EnergyBalanceDetailView to live data</name>
  <files>
    JabTracker/Views/Dashboard/DetailViews/EnergyBalanceDetailView.swift
    JabTracker/ViewModels/EnergyBalanceDetailViewModel.swift (create)
  </files>
  <action>
    Create EnergyBalanceDetailViewModel that:
    1. Fetches daily calories via MealLogService for selected period
    2. Gets TDEE from TDEEService for expenditure reference line
    3. Gets User's calorie targets for targets reference line
    4. Calculates daily balance relative to expenditure AND targets
    5. Computes balance changes at 3/7/14/30/90 day intervals
    6. Responds to displayMode toggle (expenditure vs targets)
    7. Responds to DetailTimePeriod selection

    Update EnergyBalanceDetailView to use ViewModel.
    Keep EnergyBalanceDetailData.mock for previews.
  </action>
  <verify>
    - Unit tests for dual-mode balance calculations
    - Chart shows calories consumed as bars, reference line at expenditure/target
    - Mode toggle switches reference line correctly
    - Historical log shows correct balance per day
    - Time period filter works
  </verify>
  <done>EnergyBalanceDetailView shows actual energy balance with dual-mode analysis</done>
</task>

#### Verification

- [ ] All 3 detail views compile and display real data
- [ ] SwiftUI previews still work with mock data
- [ ] Time period filtering works correctly
- [ ] ViewModels have unit test coverage
- [ ] Build succeeds, SwiftLint passes

#### Success Criteria

- Detail views show actual historical data
- Time period selector filters data correctly
- Charts render without performance issues
- Empty states handled for new users

#### Output

After completion, create `.planning/phases/34-integration-polish/34-03-SUMMARY.md`
