---
phase: 34-integration-polish
plan: 2
type: execute
---

#### Objective

Wire Standard Widgets to live data sources (TDEEService, MetricsService, MealLogService).

Purpose: Replace mock data in 4 standard grid widgets with real data
Output: Standard widgets display actual user metrics with real-time updates

#### Execution Context

./execute-phase.md
~/.claude/gsd/templates/summary.md

#### Project Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md

**Prior plan summary:**
@.planning/phases/34-integration-polish/34-01-SUMMARY.md

**Key service files:**
@JabTracker/Services/TDEEService.swift
@JabTracker/Services/MetricsService.swift
@JabTracker/Services/MealLogService.swift

**Widget files to modify:**
@JabTracker/Views/Dashboard/Widgets/ExpenditureWidget.swift
@JabTracker/Views/Dashboard/Widgets/WeightTrendWidget.swift
@JabTracker/Views/Dashboard/Widgets/EnergyBalanceWidget.swift
@JabTracker/Views/Dashboard/Widgets/GoalProgressWidget.swift

**Existing services provide:**
- `MetricsService.getWeightEntries(from:to:)` → [WeightEntry]
- `MetricsService.getLatestWeightEntry()` → WeightEntry?
- `TDEEService` → TDEE calculations
- `MealLogService.getDailyTotals(for:)` → DailyNutritionTotals

#### Tasks

<task type="auto" approach="tdd">
  <name>Wire WeightTrendWidget to live data</name>
  <files>
    JabTracker/Views/Dashboard/Widgets/WeightTrendWidget.swift
  </files>
  <action>
    Update WeightTrendWidget to:
    1. Query MetricsService.getWeightEntries(from: 7daysAgo, to: today)
    2. Query MetricsService.getLatestWeightEntry() for current weight
    3. Map WeightEntry[] to WeightTrendWidgetData.DataPoint[]
    4. Get unit preference from User model (lbs/kg)
    5. Add @State loading and handle empty state

    Keep WeightTrendWidgetData.mock for previews.
    Widget should show "No data" if no weight entries.
  </action>
  <verify>
    - Unit tests for data mapping
    - Widget shows real weight trend when entries exist
    - Empty state displays appropriately
    - Unit conversion works (lbs ↔ kg)
  </verify>
  <done>WeightTrendWidget displays actual 7-day weight history</done>
</task>

<task type="auto" approach="tdd">
  <name>Wire ExpenditureWidget to live data</name>
  <files>
    JabTracker/Views/Dashboard/Widgets/ExpenditureWidget.swift
  </files>
  <action>
    Update ExpenditureWidget to:
    1. Get current TDEE from TDEEService or User's NutritionGoal
    2. For 7-day chart, use current TDEE as baseline (TDEE doesn't change daily)
    3. Calculate average from available data
    4. Add @State loading and handle missing TDEE state

    Keep ExpenditureWidgetData.mock for previews.
    If no TDEE calculated yet, show placeholder message.
  </action>
  <verify>
    - Widget shows current TDEE estimate
    - Bar chart displays correctly
    - Handles missing TDEE gracefully
  </verify>
  <done>ExpenditureWidget displays actual TDEE estimate</done>
</task>

<task type="auto" approach="tdd">
  <name>Wire EnergyBalanceWidget to live data</name>
  <files>
    JabTracker/Views/Dashboard/Widgets/EnergyBalanceWidget.swift
  </files>
  <action>
    Update EnergyBalanceWidget to:
    1. Fetch 7 days of daily calories via MealLogService
    2. Get TDEE from TDEEService
    3. Calculate daily balance: consumed - TDEE for each day
    4. Sum for net 7-day balance
    5. Determine deficit/surplus status

    Keep EnergyBalanceWidgetData.mock for previews.
    Handle days with no food logged as 0 calories.
  </action>
  <verify>
    - Unit tests for balance calculations
    - Widget shows correct 7-day net balance
    - Deficit/surplus indicator correct
    - Handles partial data (some days missing)
  </verify>
  <done>EnergyBalanceWidget displays actual 7-day energy balance</done>
</task>

<task type="auto">
  <name>Wire GoalProgressWidget to daily nutrition progress</name>
  <files>
    JabTracker/Views/Dashboard/Widgets/GoalProgressWidget.swift
  </files>
  <action>
    Update GoalProgressWidget to show daily calorie goal progress:
    1. Get today's DailyNutritionTotals from MealLogService
    2. Get User's daily calorie target from nutritionGoals
    3. Calculate progress percentage: consumed/target × 100
    4. Cap at 100% for display (or show overflow indicator)

    Keep GoalProgressWidgetData.mock for previews.
    Clarify widget purpose with descriptive label.
  </action>
  <verify>
    - Widget shows today's calorie progress
    - Progress ring fills correctly
    - Handles 0 target gracefully
  </verify>
  <done>GoalProgressWidget displays today's calorie goal progress</done>
</task>

#### Verification

- [ ] All 4 standard widgets compile and display real data
- [ ] SwiftUI previews still work with mock data
- [ ] Empty/loading states handled appropriately
- [ ] Build succeeds, SwiftLint passes

#### Success Criteria

- Standard widget grid shows actual user metrics
- Widgets update when underlying data changes
- No crashes on empty data states
- Performance acceptable

#### Output

After completion, create `.planning/phases/34-integration-polish/34-02-SUMMARY.md`
