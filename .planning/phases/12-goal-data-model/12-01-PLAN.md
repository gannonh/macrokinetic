---
phase: 12-goal-data-model
plan: 01
type: execute
---

<objective>
Create value types and enums for nutrition program configuration.

Purpose: Establish the type-safe foundation that SwiftData models will use.
Output: ProgramConfiguration.swift with all enums, value types, and unit tests.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-goal-data-model/12-CONTEXT.md

**Codebase patterns to follow:**
- Enums with String rawValue for CloudKit (see MealSection.swift pattern)
- CaseIterable, Codable, Identifiable conformance
- displayName and description computed properties
- Value types as Codable structs for JSON storage

**Key files to reference:**
@JabTracker/Models/MealSection.swift - Enum pattern with displayName, icon, time logic
@JabTracker/Models/FoodSource.swift - Simple enum pattern
@JabTracker/Models/EntrySource.swift - Simple enum pattern

**From Phase 12 Context:**
- Program styles: Coached, Collaborative, Manual
- Diet preferences: Balanced, Low-fat, Low-carb, Keto
- Calorie floor: Standard (1538 cal), Low (1025 cal)
- Weekly distribution: Even, Shifted
- Protein levels: Low, Moderate, High, Extra High
</context>

<tasks>

<task type="auto" approach="tdd">
  <name>Task 1: Create ProgramConfiguration.swift with enums and value types</name>
  <files>
    Tests: JabTrackerTests/Models/ProgramConfigurationTests.swift
    Implementation: JabTracker/Models/ProgramConfiguration.swift
  </files>
  <action>
    **RED**: Write failing tests first:
    - Test ProgramStyle enum has 3 cases (coached, collaborative, manual)
    - Test DietPreference.macroPercentages returns correct values for each case
    - Test CalorieFloorType.minimumCalories returns 1538 for standard, 1025 for low
    - Test ProteinLevel.gramsPerKg returns correct multipliers
    - Test WeeklyCalorieDistribution.calorieTargetForDay calculates correctly
    - Test WeeklyCalorieDistribution.isValid validates sum equals 1.0

    **GREEN**: Implement ProgramConfiguration.swift:

    1. **GoalType enum** (weight_loss, maintenance, muscle_gain):
       - displayName computed property

    2. **ProgramStyle enum** (coached, collaborative, manual):
       - displayName, description computed properties

    3. **DietPreference enum** (balanced, low_fat, low_carb, keto):
       - displayName, description computed properties
       - macroPercentages: (protein: Double, carbs: Double, fat: Double)
         - Balanced: (30, 40, 30)
         - Low-fat: (30, 50, 20)
         - Low-carb: (30, 20, 50)
         - Keto: (25, 5, 70)

    4. **CalorieFloorType enum** (standard, low):
       - displayName, description computed properties
       - minimumCalories: Double (1538 for standard, 1025 for low)
       - requiresWarning: Bool (true for low)

    5. **WeeklyDistributionMode enum** (even, shifted):
       - displayName, description computed properties

    6. **ProteinLevel enum** (low, moderate, high, extra_high):
       - displayName, description computed properties
       - gramsPerKg: Double (1.2, 1.6, 2.0, 2.4)

    7. **ProgramConfiguration struct** (Codable, Equatable):
       - Properties: programStyle, dietPreference, calorieFloorType, weeklyDistributionMode, proteinLevel
       - Optional metadata: [String: String]?

    8. **WeeklyCalorieDistribution struct** (Codable, Equatable):
       - dayMultipliers: [Int: Double] (1=Sunday, 7=Saturday)
       - calorieTargetForDay(_ weekday: Int, baseCalories: Double) -> Double
       - static var even: WeeklyCalorieDistribution (empty multipliers)
       - isValid: Bool (validates weekly average equals 1.0)

    **REFACTOR**: Ensure all enums have consistent structure with displayName and description.
  </action>
  <verify>./scripts/test.sh unit 1 ProgramConfigurationTests</verify>
  <done>All ProgramConfigurationTests pass, enums have complete metadata, value types validate correctly</done>
</task>

<task type="auto">
  <name>Task 2: Build verification and lint</name>
  <files>JabTracker/Models/ProgramConfiguration.swift</files>
  <action>
    1. Run xcodegen to regenerate project with new file
    2. Run full build to verify no compilation errors
    3. Run SwiftLint to check code quality
    4. Fix any lint violations
  </action>
  <verify>
    xcodegen generate succeeds
    ./scripts/build.sh succeeds
    swiftlint shows no errors in ProgramConfiguration.swift
  </verify>
  <done>Build passes, no lint errors, file properly integrated into project</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All 6 enums created with displayName and description
- [ ] ProgramConfiguration struct encodes/decodes to JSON
- [ ] WeeklyCalorieDistribution validates correctly
- [ ] All unit tests pass
- [ ] Build succeeds
- [ ] No SwiftLint errors
</verification>

<success_criteria>
- ProgramConfigurationTests.swift exists with 6+ test cases
- ProgramConfiguration.swift has all enums and value types
- All tests pass
- Build compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/12-goal-data-model/12-01-SUMMARY.md`
</output>
