---
phase: 12-goal-data-model
plan: 02
type: execute
---

<objective>
Create SwiftData models for NutritionGoal and NutritionProgram with User integration.

Purpose: Establish the persistent data layer for goals and programs that syncs via CloudKit.
Output: Two SwiftData models, User relationship, DataController integration, and unit tests.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-goal-data-model/12-CONTEXT.md
@.planning/phases/12-goal-data-model/12-01-SUMMARY.md

**Codebase patterns to follow:**
- CloudKit-first: All properties have non-optional defaults
- Parent declares @Relationship(inverse:), child uses plain property
- Enums stored as String rawValue with computed type-safe accessor
- JSON-encoded Data for complex nested configuration (see ScheduleConfiguration)
- Timestamps: createdAt, updatedAt on all models

**Key files to reference:**
@JabTracker/Models/User.swift - Parent relationship pattern, property defaults
@JabTracker/Models/MedicationProfile.swift - Child model with parent relationship
@JabTracker/Models/DoseSchedule.swift - JSON configuration storage pattern
@JabTracker/DataController.swift - Schema registration

**From 12-01-PLAN:**
Uses enums and value types from ProgramConfiguration.swift
</context>

<tasks>

<task type="auto" approach="tdd">
  <name>Task 1: Create NutritionGoal.swift SwiftData model</name>
  <files>
    Tests: JabTrackerTests/Models/NutritionGoalTests.swift
    Implementation: JabTracker/Models/NutritionGoal.swift
  </files>
  <action>
    **RED**: Write failing tests first:
    - Test default initialization creates valid goal
    - Test goal type accessor converts to/from GoalType enum
    - Test totalWeightChangeKg calculates target - starting
    - Test estimatedWeeksToGoal calculates correctly
    - Test progressPercentage returns 0-1 range
    - Test isOnTrack returns true when targetDate is future

    **GREEN**: Implement NutritionGoal.swift:

    @Model final class NutritionGoal with properties:
    - id: UUID = UUID()
    - goalType: String = "weight_loss" (stored rawValue)
    - isActive: Bool = true
    - startingWeightKg: Double = 70.0
    - targetWeightKg: Double = 70.0
    - targetDate: Date = Date()
    - weeklyWeightChangePaceKg: Double = -0.5
    - dailyCalorieTarget: Double = 2000.0
    - dailyProteinTargetGrams: Double = 150.0
    - dailyCarbTargetGrams: Double = 200.0
    - dailyFatTargetGrams: Double = 65.0
    - lastCalculatedTDEE: Double? (for Phase 14)
    - lastTDEECalculationDate: Date? (for Phase 14)
    - initialEstimatedTDEE: Double? (for Phase 14)
    - createdAt: Date = Date()
    - updatedAt: Date = Date()
    - user: User? (plain property - child side)
    - program: NutritionProgram? with @Relationship(deleteRule: .cascade, inverse: \NutritionProgram.goal)

    Computed properties:
    - goal: GoalType (type-safe accessor)
    - totalWeightChangeKg: Double
    - estimatedWeeksToGoal: Double
    - isOnTrack: Bool
    - progressPercentage(currentWeightKg:) -> Double
    - weightChangeDisplay: String

    **REFACTOR**: Ensure init doesn't initialize relationships.
  </action>
  <verify>./scripts/test.sh unit 1 NutritionGoalTests</verify>
  <done>All NutritionGoalTests pass, model follows CloudKit patterns</done>
</task>

<task type="auto" approach="tdd">
  <name>Task 2: Create NutritionProgram.swift SwiftData model</name>
  <files>
    Tests: JabTrackerTests/Models/NutritionProgramTests.swift
    Implementation: JabTracker/Models/NutritionProgram.swift
  </files>
  <action>
    **RED**: Write failing tests first:
    - Test default initialization creates valid program
    - Test style accessor converts to/from ProgramStyle enum
    - Test diet accessor converts to/from DietPreference enum
    - Test configuration encoding/decoding roundtrip
    - Test calorieTargetForDay returns base for even distribution
    - Test calorieTargetForDay applies multiplier for shifted distribution
    - Test minimumCalories returns correct floor

    **GREEN**: Implement NutritionProgram.swift:

    @Model final class NutritionProgram with properties:
    - id: UUID = UUID()
    - programStyle: String = "coached"
    - dietPreference: String = "balanced"
    - calorieFloorType: String = "standard"
    - weeklyDistributionMode: String = "even"
    - proteinLevel: String = "moderate"
    - configurationData: Data = Data() (JSON-encoded ProgramConfiguration)
    - weeklyDistributionData: Data? (JSON-encoded WeeklyCalorieDistribution)
    - createdAt: Date = Date()
    - updatedAt: Date = Date()
    - goal: NutritionGoal? (plain property - child side)

    Type-safe accessors:
    - style: ProgramStyle
    - diet: DietPreference
    - calorieFloor: CalorieFloorType
    - distributionMode: WeeklyDistributionMode
    - protein: ProteinLevel
    - configuration: ProgramConfiguration? (decoded)

    Methods:
    - calorieTargetForDay(_ weekday: Int) -> Double?
    - private updateConfiguration() (re-encode when properties change)

    Computed properties:
    - minimumCalories: Double
    - usesCoachedMacros: Bool
    - adjustsCaloriesWithProgress: Bool

    **REFACTOR**: Ensure setters call updateConfiguration().
  </action>
  <verify>./scripts/test.sh unit 1 NutritionProgramTests</verify>
  <done>All NutritionProgramTests pass, JSON config encodes/decodes correctly</done>
</task>

<task type="auto">
  <name>Task 3: Integrate with User and DataController</name>
  <files>
    JabTracker/Models/User.swift
    JabTracker/DataController.swift
  </files>
  <action>
    1. **User.swift** - Add after line 75 (after medicationProfiles relationship):
       ```swift
       @Relationship(deleteRule: .cascade, inverse: \NutritionGoal.user)
       var nutritionGoals: [NutritionGoal]?
       ```

    2. **User.swift** - Add computed property in extension:
       ```swift
       var activeNutritionGoal: NutritionGoal? {
           nutritionGoals?.first(where: { $0.isActive })
       }
       ```

    3. **DataController.swift** - Add to schema array (line 77-89):
       ```swift
       NutritionGoal.self,
       NutritionProgram.self,
       ```

    4. Run xcodegen generate to update project
    5. Run full build to verify schema compiles
    6. Run all unit tests to verify no regressions
  </action>
  <verify>
    xcodegen generate succeeds
    ./scripts/build.sh succeeds
    ./scripts/test.sh unit 1 succeeds (all tests pass)
  </verify>
  <done>User has nutritionGoals relationship, DataController includes new models, all tests pass</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] NutritionGoal.swift exists with all properties and computed values
- [ ] NutritionProgram.swift exists with JSON config storage
- [ ] User.swift has nutritionGoals relationship
- [ ] DataController.swift schema includes both models
- [ ] All unit tests pass (NutritionGoalTests, NutritionProgramTests)
- [ ] Full test suite passes (no regressions)
- [ ] Build succeeds
</verification>

<success_criteria>
- NutritionGoal and NutritionProgram models created
- Relationship between User → NutritionGoal → NutritionProgram established
- All 3 test files pass (ProgramConfiguration, NutritionGoal, NutritionProgram)
- Build compiles without errors
- Phase 12 complete, ready for Phase 13 (Goal Configuration Wizard UI)
</success_criteria>

<output>
After completion, create `.planning/phases/12-goal-data-model/12-02-SUMMARY.md`
</output>
