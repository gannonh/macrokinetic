---
phase: 18-complete-add-burned-calories
plan: 01
type: execute
domain: ios-dev
---

<objective>
Create E2E tests for the Add Burned Calories feature, validating the CalorieExpenditureView toggle interactions and verifying the NutritionSummaryCard reflects burned calories when enabled.

Purpose: Complete Phase 18 by adding comprehensive E2E test coverage for the calorie expenditure settings implemented in the APM workflow.
Output: CalorieExpenditureUITests.swift with complete E2E test coverage for the Add Burned Calories feature.
</objective>

<execution_context>
~/.claude/gsd/workflows/execute-phase.md
~/.claude/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Codebase context (loaded from /pm-map-codebase):**
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md

**Existing implementation (completed under APM workflow):**
@JabTracker/Views/Settings/CalorieExpenditureView.swift
@JabTracker/Services/CalorieAdjustmentService.swift
@JabTracker/Views/Nutrition/NutritionSummaryViewModel.swift
@JabTracker/Services/MetricsService+HealthKit.swift

**Test patterns to follow:**
@JabTrackerUITests/MoreTabUITests.swift
@JabTrackerUITests/Utils/TestUtilities.swift

**Key accessibility identifiers already defined:**
- `calorie-expenditure-view` - Main view identifier
- `add-burned-calories-toggle` - Toggle for burned calories feature
- `predictive-activity-toggle` - Toggle for predictive activity
- `rollover-calories-toggle` - Toggle for rollover feature
- `calorie-expenditure-row` - Navigation row in More tab

**Mock active energy for testing:**
Launch argument `--mock-active-energy=250` sets active energy to 250 kcal for testing.
</context>

<tasks>

<task type="auto" approach="e2e-stub">
  <name>Task 1: Create CalorieExpenditureUITests with test stubs</name>
  <files>JabTrackerUITests/CalorieExpenditureUITests.swift</files>
  <action>
    Create E2E test file with test method stubs following established patterns:

    1. **Setup**: Use `TestUtilities.launchAppWithTestMode(resetData: true)`

    2. **Test stubs to create (empty implementations with acceptance criteria in comments):**

    Happy Path:
    - `testNavigateToCalorieExpenditureView()` - Navigate from More tab, verify view loads
    - `testAddBurnedCaloriesToggleDefaultState()` - Verify toggle exists and is off by default
    - `testToggleAddBurnedCaloriesOn()` - Toggle on, verify state persists
    - `testToggleAddBurnedCaloriesOff()` - Toggle off after being on, verify state persists

    Edge Cases:
    - `testTogglesDisabledWhenHealthSyncDisabled()` - Verify toggles are disabled when healthSyncEnabled is false
    - `testPredictiveActivityToggle()` - Toggle predictive activity, verify state
    - `testRolloverCaloriesToggle()` - Toggle rollover, verify state

    Navigation:
    - `testBackNavigationPreservesState()` - Navigate away and back, verify toggle state persists

    Follow MoreTabUITests.swift patterns for structure.
  </action>
  <verify>Build succeeds: `xcodebuild build-for-testing -project JabTracker.xcodeproj -scheme JabTracker -destination 'platform=iOS Simulator,name=iPhone 17 Pro'` completes without errors</verify>
  <done>CalorieExpenditureUITests.swift exists with 8 test method stubs, each with acceptance criteria documented in comments</done>
</task>

<task type="auto">
  <name>Task 2: Implement CalorieExpenditureUITests happy path tests</name>
  <files>JabTrackerUITests/CalorieExpenditureUITests.swift</files>
  <action>
    Implement the 4 happy path test methods:

    1. `testNavigateToCalorieExpenditureView()`:
       - Navigate to More tab
       - Tap calorie-expenditure-row
       - Verify calorie-expenditure-view exists
       - Verify add-burned-calories-toggle exists

    2. `testAddBurnedCaloriesToggleDefaultState()`:
       - Navigate to CalorieExpenditureView
       - Get toggle value (should be false/off by default for new user)
       - Assert toggle is not selected

    3. `testToggleAddBurnedCaloriesOn()`:
       - Navigate to CalorieExpenditureView
       - Tap add-burned-calories-toggle (use coordinate tap for SwiftUI toggle)
       - Verify toggle is now on (value == "1")

    4. `testToggleAddBurnedCaloriesOff()`:
       - Navigate to CalorieExpenditureView
       - Ensure toggle is on first (tap if needed)
       - Tap toggle to turn off
       - Verify toggle is now off (value == "0")

    Use established patterns:
    - `TestUtilities.navigateToTab(app, tabName: "More")`
    - `app.cells["calorie-expenditure-row"].tap()`
    - `app.switches["add-burned-calories-toggle"]`
    - Toggle tap: `toggle.coordinate(withNormalizedOffset: CGVector(dx: 0.9, dy: 0.5)).tap()`
  </action>
  <verify>Run: `./scripts/test.sh ui 1 CalorieExpenditureUITests` - All 4 happy path tests pass</verify>
  <done>4 happy path tests pass: navigate, default state, toggle on, toggle off</done>
</task>

<task type="auto">
  <name>Task 3: Implement edge case and navigation tests</name>
  <files>JabTrackerUITests/CalorieExpenditureUITests.swift</files>
  <action>
    Implement the remaining 4 test methods:

    1. `testTogglesDisabledWhenHealthSyncDisabled()`:
       - Default test user should have healthSyncEnabled = false (verify in TestDataSeeding)
       - Navigate to CalorieExpenditureView
       - Verify add-burned-calories-toggle and predictive-activity-toggle are disabled (isEnabled == false)
       - Note: rollover-calories-toggle should still be enabled (doesn't require HealthKit)

    2. `testPredictiveActivityToggle()`:
       - Navigate to CalorieExpenditureView
       - Tap predictive-activity-toggle
       - Verify toggle state changes

    3. `testRolloverCaloriesToggle()`:
       - Navigate to CalorieExpenditureView
       - Tap rollover-calories-toggle
       - Verify toggle state changes (this one doesn't require Health Sync)

    4. `testBackNavigationPreservesState()`:
       - Navigate to CalorieExpenditureView
       - Toggle rollover-calories-toggle on
       - Tap back button
       - Navigate back to CalorieExpenditureView
       - Verify rollover toggle is still on

    Note: Health-dependent toggles may be disabled in test mode without HealthKit authorization.
    Adjust test expectations accordingly based on test environment.
  </action>
  <verify>Run: `./scripts/test.sh ui 1 CalorieExpenditureUITests` - All 8 tests pass</verify>
  <done>All 8 E2E tests pass for CalorieExpenditureView</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./scripts/build.sh` succeeds without errors
- [ ] `./scripts/test.sh ui 1 CalorieExpenditureUITests` - All tests pass
- [ ] No accessibility identifier issues (elements found by expected identifiers)
- [ ] Test patterns consistent with existing MoreTabUITests
</verification>

<success_criteria>
- CalorieExpenditureUITests.swift created with 8 E2E tests
- All tests pass on iOS 26.2 simulator
- Tests follow established patterns from MoreTabUITests and TestUtilities
- No flaky tests (proper wait conditions used)
- Phase 18 complete, ready for Phase 19 (Rollover Calories)
</success_criteria>

<output>
After completion, create `.planning/phases/18-complete-add-burned-calories/18-01-SUMMARY.md`:

# Phase 18 Plan 01: Add Burned Calories E2E Tests Summary

**[One-liner summary of what was accomplished]**

## Accomplishments
- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified
- `JabTrackerUITests/CalorieExpenditureUITests.swift` - E2E tests for calorie expenditure settings

## Decisions Made
[Any test approach decisions]

## Issues Encountered
[Problems and resolutions, or "None"]

## Next Step
Phase 18 complete, ready for Phase 19: Rollover Calories
</output>
