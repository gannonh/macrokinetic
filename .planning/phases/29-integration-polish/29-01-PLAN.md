---
phase: 29-integration-polish
plan: 01
type: execute
---

<objective>
Complete the v0.6.0 onboarding flow with a polished completion screen and smooth transition to main app.

Purpose: Deliver a professional, celebratory finish to onboarding that summarizes what was configured and seamlessly hands off to the main app experience.
Output: CompletionStepView, animated transitions, and E2E test implementation for the 17-step flow.
</objective>

<execution_context>
~/.claude/gsd/workflows/execute-phase.md
~/.claude/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Prior decisions affecting this phase:**
- Mint accent color (#00A693) established in Phase 26 - use consistently
- Fullscreen onboarding (not sheet) established in Phase 26 - transition directly to ContentView
- Standard SF font (not rounded) established in Phase 26
- OnboardingView dismisses via `isPresented = false` binding → JabTrackerApp swaps to ContentView

**Current implementation:**
- `.completion` step uses PlaceholderStepView (shows "Coming in Phase 29")
- OnboardingView.swift:266-268 routes `.completion` → PlaceholderStepView
- Transition: `withAnimation(.spring()) { isPresented = false }` (line 308-309)
- No post-onboarding transition animation - immediate swap

**Relevant source files:**
@JabTracker/Onboarding/OnboardingView.swift
@JabTracker/Onboarding/OnboardingStep.swift
@JabTracker/Onboarding/OnboardingViewModel.swift
@JabTracker/Onboarding/Views/PlaceholderStepView.swift
@JabTracker/Onboarding/Views/SetupConfirmationStepView.swift
@JabTracker/JabTrackerApp.swift
@JabTrackerUITests/Onboarding/NewOnboardingUITests.swift

**Design patterns to follow:**
- SetupConfirmationStepView: Header with icon, title, subtitle, card sections
- Permission step views: BenefitsCard component for benefit lists
- CalculatingOverlayView: Animated icons with pulse/rotation effects
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CompletionStepView</name>
  <files>
    JabTracker/Onboarding/Views/CompletionStepView.swift
    JabTracker/Onboarding/OnboardingView.swift
  </files>
  <action>
    Create CompletionStepView.swift with celebratory finish:

    **Visual elements:**
    - Animated checkmark with success animation (scale + opacity pulse)
    - SF Symbol: "checkmark.circle.fill" with green gradient (match SetupConfirmationStepView header)
    - Title: "You're All Set!" (from OnboardingStep.title)
    - Subtitle: "Let's start your journey together" (from OnboardingStep.subtitle)

    **Summary section (card with what was configured):**
    - Goal type (weight loss/maintain/gain) with target
    - Program style (Coached/Collaborative/Manual)
    - Daily calorie target
    - Use SummaryRow component from Design/ folder

    **Next steps guidance:**
    - "Here's how to get started:" with 3 quick tips:
      1. "Log your first meal to start tracking"
      2. "Check in weekly to adjust your targets"
      3. "Enable notifications for reminders"
    - Use BenefitsCard pattern but with numbered steps

    **Layout:**
    - ScrollView for long content
    - DesignTokens.Colors.groupedBackground
    - accessibilityIdentifier("onboarding-completion-step")

    **Update OnboardingView.swift routing:**
    - Change line 266-268 from `PlaceholderStepView(step: step)` to `CompletionStepView(viewModel: viewModel)`

    **Do NOT:**
    - Add confetti or heavy particle animations (keep it clean/professional)
    - Include any new external dependencies
    - Change the button text (stays "Get Started" from navigationButtons)
  </action>
  <verify>Build succeeds, completion step displays with summary and next steps</verify>
  <done>CompletionStepView shows animated checkmark, personalized summary, and next steps guidance</done>
</task>

<task type="auto">
  <name>Task 2: Add onboarding → main app transition animation</name>
  <files>
    JabTracker/JabTrackerApp.swift
    JabTracker/Onboarding/OnboardingView.swift
  </files>
  <action>
    Add smooth fade/slide transition between onboarding and ContentView:

    **In JabTrackerApp.swift:**
    - Wrap the `if showingOnboarding` conditional in a transition container
    - Add `.transition(.opacity.combined(with: .move(edge: .leading)))` to OnboardingView
    - Add `.transition(.opacity.combined(with: .move(edge: .trailing)))` to ContentView
    - Wrap state change in `withAnimation(.easeInOut(duration: 0.4))` if needed

    **Pattern from OnboardingView step transitions:**
    ```swift
    .transition(
        .asymmetric(
            insertion: .move(edge: .trailing).combined(with: .opacity),
            removal: .move(edge: .leading).combined(with: .opacity)
        )
    )
    ```

    **Alternative simpler approach:**
    - Just ensure `withAnimation(.spring())` in completeOnboarding() is sufficient
    - The fullscreen swap might already animate - verify first before adding complexity

    **Test approach first:** Run app in simulator, complete onboarding, observe transition.
    Only add explicit transitions if the current swap is jarring.

    **Do NOT:**
    - Add delays or artificial loading states
    - Change the fundamental `showingOnboarding` binding approach
    - Break the biometric lock overlay positioning
  </action>
  <verify>Manual test: Complete onboarding, observe smooth transition to main app (no jarring swap)</verify>
  <done>Onboarding → main app transition is smooth (fade, slide, or cross-dissolve)</done>
</task>

<task type="auto" approach="e2e-stub">
  <name>Task 3: Implement E2E test for complete onboarding flow</name>
  <files>
    JabTrackerUITests/Onboarding/NewOnboardingUITests.swift
  </files>
  <action>
    Update existing NewOnboardingUITests.swift to implement the stub tests:

    **testCompleteNewOnboardingFlow:**
    1. Launch with `["--ui-testing", "--reset-app-data", "--force-onboarding"]`
    2. Verify welcome step displays (identifier: "onboarding-view")
    3. Navigate through the 17-step flow:
       - welcome → Continue
       - uspShowcase → Continue
       - healthKit → "Not Now" (skip permission in testing)
       - goalType → Select "Weight Loss" → Continue
       - targetWeight → Continue (use defaults)
       - profileCompletion → Continue (if shown - HealthKit may skip it)
       - programStyle → Select "Coached" → Continue
       - dietPreference → Select "Balanced" → Continue
       - calorieFloor → Continue (use default)
       - activityLevel → Continue
       - weeklyDistribution → Continue
       - proteinLevel → Continue
       - setupConfirmation → Continue
       - faceID → "Not Now"
       - notifications → "Not Now"
       - completion → Verify displays, tap "Get Started"
    4. Verify main app displays (identifier: "content-view" or tab bar exists)

    **Test utilities needed:**
    - Helper to tap Continue button: `app.buttons["onboarding-continue-button"]`
    - Helper for permission skip: `app.buttons.matching(identifier: "skip-button").firstMatch` or label "Not Now"
    - Selection helpers for goal/program options

    **Note on dynamic steps:**
    - profileCompletion may be skipped if HealthKit provides profile data
    - shiftedDaySelection only shows if shifted distribution selected
    - Test the happy path (no HealthKit data, even distribution)

    **Do NOT:**
    - Try to actually request system permissions (use skip buttons)
    - Test every possible step combination (just happy path)
    - Add excessive waits - use waitForExistence with reasonable timeouts
  </action>
  <verify>./scripts/test.sh ui 1 NewOnboardingUITests/testCompleteNewOnboardingFlow passes</verify>
  <done>E2E test navigates through 17-step onboarding and verifies completion</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Build succeeds: `./scripts/build.sh`
- [ ] SwiftLint passes: `swiftlint`
- [ ] Unit tests pass: `./scripts/test.sh unit 1`
- [ ] E2E test passes: `./scripts/test.sh ui 1 NewOnboardingUITests/testCompleteNewOnboardingFlow`
- [ ] Manual verification: Complete onboarding flow, verify completion screen and transition
</verification>

<success_criteria>
- CompletionStepView displays with animated checkmark, personalized summary, and next steps
- Transition from onboarding to main app is smooth (not jarring)
- E2E test covers the complete 17-step flow
- No new SwiftLint violations
- Phase 29 complete, v0.6.0 milestone ready for final review
</success_criteria>

<output>
After completion, create `.planning/phases/29-integration-polish/29-01-SUMMARY.md`:

# Phase 29 Plan 1: Integration & Polish Summary

**[Substantive one-liner about what shipped]**

## Performance
- Duration: X min
- Started: [timestamp]
- Completed: [timestamp]
- Tasks: 3
- Files modified: X

## Accomplishments
- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified
- `path/to/file.swift` - Description

## Decisions Made
[Key decisions and rationale, or "None"]

## Deviations from Plan
[Changes from plan with reasoning, or "None"]

## Issues Encountered
[Problems and resolutions, or "None"]

## Next Step
Phase 29 complete. v0.6.0 Onboarding Redux milestone complete.
Ready for `/pm-complete-milestone` or PR review.
</output>
