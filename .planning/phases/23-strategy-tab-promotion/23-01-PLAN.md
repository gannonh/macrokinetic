---
phase: 23-strategy-tab-promotion
plan: 1
type: execute
domain: ios-dev
---

<objective>
Replace Shots tab with Strategy in the main tab bar, promoting Goals & Strategy to top-level prominence.

Purpose: Elevate nutrition strategy management to a primary tab (replacing GLP-1 shots now consolidated under More)
Output: Strategy appears as top-level tab with target icon, removed from More overflow menu
</objective>

<execution_context>
~/.claude/gsd/workflows/execute-phase.md
~/.claude/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Prior phase context:**
@.planning/phases/22-glp1-programs-consolidation/22-02-SUMMARY.md

**Prior decisions affecting this phase:**
- Phase 22 kept "Goals & Strategy" as separate More row specifically for Phase 23 promotion
- GLP-1 Programs now consolidated under More via GLP1ProgramsView

**Key source files:**
@JabTracker/Models/Tab.swift
@JabTracker/ContentView.swift
@JabTracker/Views/More/MoreView.swift
@JabTracker/Views/Strategy/StrategyView.swift

**Codebase patterns to follow:**
- Tab enum defines all tabs with title/icon computed properties
- ContentView uses Tab enum for TabView wiring with .tabItem and .tag
- StrategyView already has its own NavigationStack (ready for top-level)
- Dashboard promotion pattern: View directly in TabView, PageHeader in safeAreaInset
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Tab enum to replace Shots with Strategy</name>
  <files>JabTracker/Models/Tab.swift</files>
  <action>
    Rename the `.shots` case to `.strategy`:
    1. Change `case shots` to `case strategy`
    2. Update title: `.strategy: return "Strategy"` (was "Shots")
    3. Update icon: `.strategy: return "target"` (was "syringe")
    4. Maintain same position in `mainTabs` array (4th position)

    **Why target icon:** Consistent with StrategyView's existing iconography (target for goals/strategy)
  </action>
  <verify>Build succeeds (`./scripts/build.sh`) - Tab.swift compiles without errors</verify>
  <done>Tab.swift has `.strategy` case with "Strategy" title and "target" icon</done>
</task>

<task type="auto">
  <name>Task 2: Update ContentView to show StrategyView in tab bar</name>
  <files>JabTracker/ContentView.swift</files>
  <action>
    Replace ShotsView with StrategyView in the TabView:
    1. Replace `ShotsView()` with `StrategyView()`
    2. Update `.tabItem { Label(Tab.shots.title...` to `Tab.strategy.title`
    3. Update `.tag(Tab.shots)` to `.tag(Tab.strategy)`

    **Pattern to follow (Dashboard example at line 42-46):**
    ```swift
    StrategyView()
        .tabItem {
            Label(Tab.strategy.title, systemImage: Tab.strategy.icon)
        }
        .tag(Tab.strategy)
    ```

    **Note:** StrategyView already has its own NavigationStack, so no wrapper needed.
  </action>
  <verify>Build succeeds (`./scripts/build.sh`) - ContentView compiles without errors</verify>
  <done>ContentView shows StrategyView at Tab.strategy position with correct tab item</done>
</task>

<task type="auto">
  <name>Task 3: Remove Goals & Strategy from MoreView overflow menu</name>
  <files>JabTracker/Views/More/MoreView.swift</files>
  <action>
    Remove the NavigationLink for "Goals & Strategy" from the overflow section:
    1. Delete lines 29-32 (NavigationLink to StrategyView with "Goals & Strategy" label)
    2. Keep GLP-1 Programs and Food Library in the overflow section

    **Before (lines 24-41):**
    ```swift
    Section {
        NavigationLink(destination: GLP1ProgramsView()) { ... }
        NavigationLink(destination: StrategyView()) { ... }  // DELETE THIS
        Button { showingFoodLibrary = true } { ... }
    }
    ```

    **After:**
    ```swift
    Section {
        NavigationLink(destination: GLP1ProgramsView()) { ... }
        Button { showingFoodLibrary = true } { ... }
    }
    ```
  </action>
  <verify>Build succeeds (`./scripts/build.sh`) - MoreView compiles without errors</verify>
  <done>MoreView no longer shows "Goals & Strategy" row - Strategy is now a top-level tab</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `./scripts/build.sh` succeeds without errors
- [ ] `swiftlint` passes (no new violations)
- [ ] Run app in simulator: Strategy tab appears in tab bar with target icon
- [ ] Strategy tab shows StrategyView content (check-in countdown, program cards)
- [ ] More tab no longer shows "Goals & Strategy" row
- [ ] GLP-1 Programs still accessible from More tab
- [ ] Navigation within Strategy tab works (edit goal, new program sheets)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Strategy tab replaces Shots tab in main tab bar
- StrategyView accessible via Strategy tab tap
- "Goals & Strategy" removed from More overflow menu
- No TypeScript/Swift errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/23-strategy-tab-promotion/23-01-SUMMARY.md`:

# Phase 23 Plan 01: Strategy Tab Promotion Summary

**[Substantive one-liner - what shipped]**

## Performance

- **Duration:** X min
- **Tasks:** 3
- **Files modified:** 3

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `JabTracker/Models/Tab.swift` - Renamed .shots → .strategy with target icon
- `JabTracker/ContentView.swift` - Replaced ShotsView with StrategyView in TabView
- `JabTracker/Views/More/MoreView.swift` - Removed Goals & Strategy NavigationLink

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Phase 23 complete, ready for Phase 24 (Add Button Redesign).
</output>
