---
phase: 02-create-food-ui
plan: 02
type: execute
domain: ios-dev
created: 2025-12-22T20:28:46Z
---

<objective>
Wire "To Custom" button in FoodDetailSheet and implement "Create & Add" flow.

Purpose: Complete the user journey from viewing a food to creating a custom version and optionally logging it.
Output: Functional "To Custom" button, "Create & Add" saves and logs food in one action.
</objective>

<execution_context>
~/.claude/gsd/workflows/execute-phase.md
~/.claude/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-create-food-ui/02-01-SUMMARY.md

**Prior decisions affecting this phase:**
- Phase 1: CustomFoodService.createCustomFood() returns the created Food
- Phase 1: Food model has all fields needed for MealLogService.logFood()

**Dependencies from 02-01:**
- CreateFoodViewModel with save() method returning Food
- CreateFoodSheet with prefillFrom parameter
- onFoodCreated callback for navigation

**Key files to modify:**
@JabTracker/Views/Nutrition/FoodDetailSheet.swift - Wire "To Custom" button (line ~298-309)
@JabTracker/Views/Nutrition/CreateFoodSheet.swift - Add "Create & Add" button
@JabTracker/Services/MealLogService.swift - Used for logging after create
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire "To Custom" button and sheet presentation</name>
  <files>
    JabTracker/Views/Nutrition/FoodDetailSheet.swift
    JabTracker/Views/Nutrition/CreateFoodSheet.swift
  </files>
  <action>
    **In FoodDetailSheet:**
    1. Add @State for sheet presentation:
       ```swift
       @State private var showingCreateCustom = false
       ```

    2. Update "To Custom" button (around line 298-309):
       - Remove `.disabled(true)`
       - Add action to set `showingCreateCustom = true`
       - Add accessibility identifier "to-custom-button"

    3. Add sheet modifier:
       ```swift
       .sheet(isPresented: $showingCreateCustom) {
           CreateFoodSheet(
               customFoodService: AppServices.shared.customFoodService!,
               prefillFrom: food,
               onFoodCreated: { createdFood in
                   // Optionally navigate or show success
               },
               onDismiss: {
                   // Optional cleanup
               }
           )
       }
       ```

    **In CreateFoodSheet:**
    1. Add "Create & Add" button in footer/toolbar:
       - Primary action button (borderedProminent style)
       - Calls `saveAndAdd()` which:
         a. Creates custom food via viewModel.save()
         b. Logs it via MealLogService.logFood() with current meal/time
         c. Dismisses sheet and calls onFoodCreated

    2. Add required properties for "Create & Add":
       ```swift
       let mealLogService: MealLogService?
       let selectedMeal: MealSection
       let selectedTime: Date
       ```

    3. Implement saveAndAdd():
       ```swift
       private func saveAndAdd() async {
           do {
               let food = try await viewModel.save()
               if let mealLogService {
                   _ = try await mealLogService.logFood(
                       food: food,
                       servingGrams: viewModel.servingSize,
                       mealSection: selectedMeal,
                       notes: nil,
                       loggedAt: selectedTime
                   )
               }
               onFoodCreated?(food)
               dismiss()
           } catch {
               viewModel.errorMessage = error.localizedDescription
               viewModel.showingError = true
           }
       }
       ```

    4. Update toolbar to have two actions:
       - "Create" (trailing) - saves food only, dismisses
       - Footer button "Create & Add" - saves and logs, dismisses
  </action>
  <verify>Build succeeds, "To Custom" button opens CreateFoodSheet with pre-filled data</verify>
  <done>"To Custom" navigates to CreateFoodSheet, both "Create" and "Create & Add" work correctly</done>
</task>

<task type="auto" approach="e2e-stub">
  <name>Task 2: Stub E2E tests for Create Food flow</name>
  <files>JabTrackerUITests/Nutrition/CreateFoodUITests.swift</files>
  <action>
    Create test file with method stubs (NO implementation yet):

    ```swift
    import XCTest

    final class CreateFoodUITests: XCTestCase {

        override func setUpWithError() throws {
            continueAfterFailure = false
        }

        // MARK: - Happy Path

        /// User can open CreateFoodSheet from "To Custom" button
        /// Acceptance: Sheet appears with pre-filled food data
        func testUserCanOpenCreateFoodFromToCustom() {
            // TODO: Implement after manual smoke test
        }

        /// User can create a custom food with valid data
        /// Acceptance: Food saved, sheet dismisses, no errors
        func testUserCanCreateCustomFoodWithValidData() {
            // TODO: Implement after manual smoke test
        }

        /// User can use "Create & Add" to save and log food
        /// Acceptance: Food created AND logged to current meal
        func testUserCanCreateAndAddFood() {
            // TODO: Implement after manual smoke test
        }

        // MARK: - Validation

        /// Save button is disabled when name is empty
        /// Acceptance: Button disabled, cannot tap
        func testSaveDisabledWhenNameEmpty() {
            // TODO: Implement after manual smoke test
        }

        /// User sees error for invalid macro values
        /// Acceptance: Error alert shown for negative values
        func testUserSeesErrorForInvalidMacros() {
            // TODO: Implement after manual smoke test
        }

        // MARK: - Edit Mode

        /// User can edit an existing custom food
        /// Acceptance: Form shows existing values, save updates food
        func testUserCanEditExistingCustomFood() {
            // TODO: Implement after manual smoke test
        }
    }
    ```

    **Requirements for stubs:**
    - Descriptive method names that explain the scenario
    - Acceptance criteria documented in comments
    - Empty implementation body with TODO comment
    - Organized by MARK sections (Happy Path, Validation, Edit Mode)
  </action>
  <verify>Build succeeds, test file exists with all stubs</verify>
  <done>E2E test stubs created with acceptance criteria documented</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./scripts/build.sh` succeeds without errors
- [ ] "To Custom" button in FoodDetailSheet is enabled and opens CreateFoodSheet
- [ ] CreateFoodSheet pre-fills data from source food
- [ ] "Create" saves food and dismisses
- [ ] "Create & Add" saves food, logs to meal, and dismisses
- [ ] E2E test stubs compile successfully
</verification>

<success_criteria>

- "To Custom" button functional in FoodDetailSheet
- CreateFoodSheet integrates with FoodDetailSheet flow
- "Create & Add" flow saves and logs in one action
- E2E test stubs ready for implementation after smoke test
- Phase 2 complete - all roadmap deliverables met
</success_criteria>

<output>
After completion, create `.planning/phases/02-create-food-ui/02-02-SUMMARY.md`:

# Phase 2 Plan 2: Integration & Testing Summary

**[Substantive one-liner - what shipped]**

## Performance

- **Duration:** X min
- **Started:** YYYY-MM-DDTHH:MM:SSZ
- **Completed:** YYYY-MM-DDTHH:MM:SSZ
- **Tasks:** 2
- **Files modified:** X

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `path/to/file.swift` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Phase 2 complete, ready for Phase 3: Food Library Integration
</output>
