---
phase: 22-glp1-programs-consolidation
plan: 01
type: execute
domain: ios-dev
---

<objective>
Extract composable section components from ShotsView and refactor ShotsView to use them.

Purpose: Create reusable section components that can be used in both ShotsView (existing) and GLP1ProgramsView (Plan 02).
Output: Three new section components + refactored ShotsView that uses them with identical behavior.
</objective>

<execution_context>
~/.claude/gsd/workflows/execute-phase.md
~/.claude/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md

**Relevant source files:**
@JabTracker/Views/Shots/ShotsView.swift
@JabTracker/ViewModels/AnalyticsViewModel.swift
@JabTracker/Views/Analytics/ConcentrationTimelineChart.swift
@JabTracker/Views/Analytics/AdherenceMetricsCard.swift
@JabTracker/Views/History/DoseHistoryView.swift
@JabTracker/Views/History/DoseCalendarView.swift

**Architecture approach:** Clean Architecture (Option B)
- Extract composable sections from ShotsView
- Each section accepts props for data/services
- ShotsView refactored to use extracted sections
- Sections reusable in GLP1ProgramsView (Plan 02)
</context>

<tasks>

<task type="auto" approach="tdd">
  <name>Task 1: Create ConcentrationSection component</name>
  <files>
    Tests: JabTrackerTests/Views/Analytics/ConcentrationSectionTests.swift
    Implementation: JabTracker/Views/Analytics/ConcentrationSection.swift
  </files>
  <action>
    Extract concentration chart section from ShotsView lines 145-158 into reusable component.

    **RED**: Write tests for ConcentrationSection:
    - Test: Renders ConcentrationTimelineChart when dataset exists
    - Test: Renders loading view when isLoadingData is true and dataset is nil
    - Test: Renders noDataSection when user is nil or profiles empty

    **GREEN**: Create ConcentrationSection.swift:
    ```swift
    struct ConcentrationSection: View {
        let user: User?
        let medicationProfiles: [MedicationProfile]
        let viewModel: AnalyticsViewModel
        let isLoadingData: Bool

        var body: some View { ... }
    }
    ```

    Include:
    - Props for user, medicationProfiles, viewModel, isLoadingData
    - chartLoadingView() helper (move from ShotsView)
    - noDataSection view (move from ShotsView)
    - accessibilityIdentifier("concentration-section")

    **REFACTOR**: Ensure clean code with proper MARK sections.
  </action>
  <verify>./scripts/test.sh unit 1 ConcentrationSectionTests && ./scripts/build.sh</verify>
  <done>ConcentrationSection component created, tests pass, builds without errors</done>
</task>

<task type="auto" approach="tdd">
  <name>Task 2: Create AdherenceSection component</name>
  <files>
    Tests: JabTrackerTests/Views/Analytics/AdherenceSectionTests.swift
    Implementation: JabTracker/Views/Analytics/AdherenceSection.swift
  </files>
  <action>
    Extract adherence metrics section from ShotsView lines 160-208 into reusable component.

    **RED**: Write tests for AdherenceSection:
    - Test: Renders all adherence cards when user and profiles exist
    - Test: Renders noDataSection when user is nil
    - Test: Renders noDataSection when profiles is empty

    **GREEN**: Create AdherenceSection.swift:
    ```swift
    struct AdherenceSection: View {
        let user: User?
        let medicationProfiles: [MedicationProfile]
        let viewModel: AnalyticsViewModel
        let modelContext: ModelContext
        let analyticsService: AnalyticsService

        var body: some View { ... }
    }
    ```

    Include:
    - Props for user, medicationProfiles, viewModel, modelContext, analyticsService
    - All adherence cards: AdherenceMetricsCard, StreakCounterView, AdherenceProgressIndicator, AdherenceTrendChart, MissedDosePatternView
    - adherenceRate() helper function
    - noDataSection view (can share with ConcentrationSection via extension or copy)
    - accessibilityIdentifier("adherence-section")

    **REFACTOR**: Extract adherenceRate calculation to method.
  </action>
  <verify>./scripts/test.sh unit 1 AdherenceSectionTests && ./scripts/build.sh</verify>
  <done>AdherenceSection component created, tests pass, builds without errors</done>
</task>

<task type="auto">
  <name>Task 3: Create HistorySection and refactor ShotsView</name>
  <files>
    JabTracker/Views/History/HistorySection.swift
    JabTracker/Views/Shots/ShotsView.swift
  </files>
  <action>
    1. Create HistorySection.swift extracting lines 210-220 from ShotsView:
    ```swift
    struct HistorySection: View {
        let selectedMode: ShotsView.HistoryMode

        var body: some View {
            switch selectedMode {
            case .list:
                DoseHistoryView()
                    .accessibilityIdentifier("dose-history-container")
            case .calendar:
                DoseCalendarView()
                    .accessibilityIdentifier("dose-calendar-container")
            }
        }
    }
    ```

    2. Move HistoryMode enum to separate file or make it public for reuse.

    3. Refactor ShotsView to use extracted sections:
    - Import new section components
    - Replace concentrationContent with ConcentrationSection(...)
    - Replace adherenceContent with AdherenceSection(...)
    - Replace historyContent with HistorySection(...)
    - Remove noDataSection and chartLoadingView (now in sections)
    - Keep all state management, data loading, controls unchanged

    4. Run xcodegen generate after creating new files.

    **Critical**: ShotsView behavior must be IDENTICAL after refactor. This is a pure extraction, no behavior changes.
  </action>
  <verify>xcodegen generate && ./scripts/build.sh && ./scripts/test.sh unit 1</verify>
  <done>HistorySection created, ShotsView refactored to use all three sections, all tests pass, behavior unchanged</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./scripts/build.sh` succeeds without errors
- [ ] `./scripts/test.sh unit 1` passes all tests
- [ ] `swiftlint` passes without new violations
- [ ] Manual test: Shots tab displays correctly (all 3 sections work)
- [ ] Manual test: Concentration chart loads and time period filter works
- [ ] Manual test: Adherence cards display correctly
- [ ] Manual test: History list/calendar toggle works
</verification>

<success_criteria>
- All three section components created (ConcentrationSection, AdherenceSection, HistorySection)
- ShotsView refactored to use extracted sections
- Tests written for section components
- No behavior changes in ShotsView
- Build passes, tests pass, lint passes
</success_criteria>

<output>
After completion, create `.planning/phases/22-glp1-programs-consolidation/22-01-SUMMARY.md`:

# Phase 22 Plan 01: Section Extraction Summary

**[Substantive one-liner about what was accomplished]**

## Accomplishments
- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified
- `JabTracker/Views/Analytics/ConcentrationSection.swift` - New composable section
- `JabTracker/Views/Analytics/AdherenceSection.swift` - New composable section
- `JabTracker/Views/History/HistorySection.swift` - New composable section
- `JabTracker/Views/Shots/ShotsView.swift` - Refactored to use sections
- `JabTrackerTests/Views/Analytics/ConcentrationSectionTests.swift` - Tests
- `JabTrackerTests/Views/Analytics/AdherenceSectionTests.swift` - Tests

## Decisions Made
[Key decisions and rationale, or "None"]

## Issues Encountered
[Problems and resolutions, or "None"]

## Next Step
Ready for 22-02-PLAN.md (GLP1ProgramsView & Integration)
</output>
