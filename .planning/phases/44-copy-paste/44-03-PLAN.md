---
phase: 44-copy-paste
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - JabTracker/Views/FoodLog/FoodLogView.swift
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Paste confirmation dialog appears anchored near the paste button in header toolbar"
    - "Context menu paste dialog positioning remains unchanged (already working)"
  artifacts:
    - path: "JabTracker/Views/FoodLog/FoodLogView.swift"
      provides: "Paste confirmation dialog with correct anchor positioning"
      contains: "confirmationDialog"
  key_links:
    - from: "CopyPasteSegmentedControl or header HStack"
      to: "confirmationDialog"
      via: "modifier attachment point"
      pattern: "\\.confirmationDialog"
---

<objective>
Fix paste confirmation dialog positioning so it anchors near the paste button in the header toolbar instead of at the bottom of the view.

Purpose: UAT Test 6 reported the confirmation dialog appears "near the bottom of the view, nowhere near the button" when using the paste button. This is a minor UX issue but the user wants it fixed.

Output: Updated FoodLogView.swift with confirmationDialog attached to the header area rather than the NavigationStack.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-copy-paste/44-UAT.md

@JabTracker/Views/FoodLog/FoodLogView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move confirmationDialog attachment to header area</name>
  <files>JabTracker/Views/FoodLog/FoodLogView.swift</files>
  <action>
The `.confirmationDialog` modifier for paste (lines 256-278) is currently attached to the `NavigationStack`, causing it to position at the bottom of the entire view. Move it to attach to the header HStack or the CopyPasteSegmentedControl so it anchors near the paste button.

Steps:
1. Remove the `.confirmationDialog("Paste Foods", ...)` modifier from lines 256-278 (currently after `.toolbar(.hidden, for: .navigationBar)`)
2. Add the same `.confirmationDialog` modifier to the HStack inside `PageHeader` (around line 173-211), attached directly to the HStack or to the CopyPasteSegmentedControl

The dialog content stays the same:
- Title: "Paste Foods" with titleVisibility: .visible
- Buttons: "Add to Existing", "Replace Existing" (destructive), "Cancel"
- Message: Shows item count and target date

This change only affects WHERE the dialog anchors, not its behavior. Context menu pasting (which works correctly) is not affected because those context menus have their own presentation context.
  </action>
  <verify>
Build succeeds: `./scripts/build.sh`
  </verify>
  <done>
Paste confirmation dialog now appears anchored near the paste button in the header toolbar when using the segmented control paste option. Context menu paste behavior unchanged.
  </done>
</task>

</tasks>

<verification>
1. Build: `./scripts/build.sh` passes
2. Manual test: Copy a day with entries, navigate to a day with existing entries, tap paste button in header - dialog should appear anchored near the top of the screen (near the button), not at the bottom
3. Manual test: Same scenario using context menu (long-press macro card) - dialog should still work correctly
</verification>

<success_criteria>
- Build passes without errors
- Paste button in header toolbar shows confirmation dialog near the button (upper area of screen)
- Context menu paste continues to work as before
</success_criteria>

<output>
After completion, create `.planning/phases/44-copy-paste/44-03-SUMMARY.md`
</output>
