---
phase: 35.1-food-search-header-indicators
plan: 01
type: execute
domain: ios-dev
---

<objective>
Update Food Search sheet header to show kcal and protein remaining indicators matching the Food Log view style (progress bars with "X left" labels).

Purpose: Create visual consistency between Food Log and Food Search views, helping users track their remaining macros while searching for foods.
Output: FoodSearchSheet header with MacroProgressBar components showing calories and protein progress.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase context
@.planning/phases/35-search-performance-ux/35-01-SUMMARY.md

# Key source files
@JabTracker/Views/Nutrition/FoodSearchSheet+Sections.swift
@JabTracker/ViewModels/FoodSearchSheetViewModel.swift
@JabTracker/Design/MacroProgressBar.swift
@JabTracker/Views/FoodLog/MacroSummaryConsumedPage.swift

**Established patterns:**
- MacroProgressBar component with displayMode: .fraction and showRemainingBelow: true
- ViewModel exposes consumed/target/remaining values for macro calculations
- @Observable ViewModels with @MainActor

**Visual reference:**
The indicators should match the Food Log card style:
- Flame icon + "consumed/target" fraction + progress bar + "X left" below
- "P" label + "consumed/target" fraction + progress bar + "X left" below
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add consumed/target macro properties to ViewModel</name>
  <files>JabTracker/ViewModels/FoodSearchSheetViewModel.swift</files>
  <action>
Update FoodSearchSheetViewModel to expose consumed and target values alongside the existing remaining values:

1. Add new properties in the "Daily Tracking" section:
   - `consumedCalories: Double = 0`
   - `targetCalories: Double = 0`
   - `consumedProtein: Double = 0`
   - `targetProtein: Double = 0`

2. Update `loadInitialData(user:for:)` to populate these values:
   - Set targetCalories from `user.dailyCalorieGoal`
   - Set targetProtein from `user.dailyProteinGoal`
   - Set consumedCalories from `totals.calories`
   - Set consumedProtein from `totals.protein`
   - Keep existing remainingCalories and remainingProtein calculations

Note: Do NOT change the existing remainingCalories/remainingProtein properties - they are still used by the current header implementation until Task 2 replaces it.
  </action>
  <verify>Build succeeds: `./scripts/build.sh`</verify>
  <done>ViewModel exposes consumedCalories, targetCalories, consumedProtein, targetProtein alongside existing remaining values</done>
</task>

<task type="auto">
  <name>Task 2: Replace header indicators with MacroProgressBar components</name>
  <files>JabTracker/Views/Nutrition/FoodSearchSheet+Sections.swift</files>
  <action>
Replace the "Remaining macros display" section in headerSection (lines 237-258) with MacroProgressBar components:

1. Import the MacroProgressBar (already available in Design/)

2. Replace the existing HStack containing VStacks with:
```swift
HStack(spacing: 12) {
    MacroProgressBar(
        label: "",
        current: viewModel.consumedCalories,
        target: viewModel.targetCalories,
        color: DesignTokens.Colors.calories,
        displayMode: .fraction,
        icon: "flame.fill",
        showRemainingBelow: true,
        accessibilityIdentifier: "search-header-calories"
    )
    .frame(width: 80)

    MacroProgressBar(
        label: "P",
        current: viewModel.consumedProtein,
        target: viewModel.targetProtein,
        color: DesignTokens.Colors.protein,
        displayMode: .fraction,
        showRemainingBelow: true,
        accessibilityIdentifier: "search-header-protein"
    )
    .frame(width: 70)
}
```

3. Remove the Divider that was between the old VStacks

4. Adjust spacing if needed to fit within the header layout - the progress bars should be right-aligned and compact

Note: Keep the close button and time picker button unchanged. Only replace the macro indicators on the right side.
  </action>
  <verify>Build succeeds: `./scripts/build.sh`</verify>
  <done>Header shows MacroProgressBar components with fraction display, progress bars, and "X left" labels matching Food Log style</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Food Search sheet header with MacroProgressBar indicators for calories and protein</what-built>
  <how-to-verify>
    1. Run the app in Xcode or Simulator
    2. Navigate to Food Log tab
    3. Tap the + button to open Food Search sheet
    4. Verify header shows:
       - Left side: X close button, time picker
       - Right side: Calories indicator (flame icon, fraction, progress bar, "X left")
       - Right side: Protein indicator (P label, fraction, progress bar, "X left")
    5. Compare with Food Log card indicators - style should match
    6. Verify indicators update correctly based on logged foods
    7. Test with no foods logged (should show full target remaining)
    8. Test with some foods logged (progress bar should partially fill)
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `./scripts/build.sh` succeeds without errors
- [ ] No new SwiftLint warnings introduced
- [ ] Header indicators match Food Log style visually
- [ ] Progress bars reflect actual consumed vs target values
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Food Search header shows MacroProgressBar indicators matching Food Log style
- Phase 35.1 complete
</success_criteria>

<output>
After completion, create `.planning/phases/35.1-food-search-header-indicators/35.1-01-SUMMARY.md`:

# Phase 35.1-01: Food Search Header Indicators Summary

**[Substantive one-liner - what shipped]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `path/to/file.ts` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Phase complete, ready for Phase 36: Search Ranking & Recall
</output>
