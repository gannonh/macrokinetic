---
phase: 21-integration-polish
plan: 2
type: execute
domain: ios-dev
---

<objective>
Complete E2E test infrastructure for rollover and predictive activity features, then verify all Phase 21 work.

Purpose: Fill the E2E test coverage gaps identified in Phases 19-20 where tests were stubbed pending manual smoke test verification.
Output: Working E2E tests for RolloverCaloriesUITests (5 tests) and PredictiveActivityUITests (6 tests) with proper seeding infrastructure, plus final verification of all Phase 21 work.
</objective>

<execution_context>
~/.claude/gsd/workflows/execute-phase.md
~/.claude/gsd/templates/summary.md
~/.claude/gsd/references/checkpoints.md
~/.claude/skills/ios-e2e-testing/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md

**Prior plan summary:**
@.planning/phases/21-integration-polish/21-01-SUMMARY.md

**Prior phase summaries:**
@.planning/phases/18-complete-add-burned-calories/18-01-SUMMARY.md
@.planning/phases/19-rollover-calories/19-01-SUMMARY.md
@.planning/phases/20-predictive-activity-adjustment/20-01-SUMMARY.md

**Relevant source files:**
@JabTracker/JabTrackerApp.swift
@JabTracker/DataController.swift
@JabTracker/Utils/TestDataSeeding.swift
@JabTrackerUITests/CalorieExpenditureUITests.swift
@JabTrackerUITests/RolloverCaloriesUITests.swift
@JabTrackerUITests/PredictiveActivityUITests.swift

**Prior decisions affecting this phase:**
- Phase 18: E2E test pattern established with `--seed-calorie-user` and `--mock-active-energy=X` launch arguments
- Phase 19: RolloverCaloriesUITests created with 5 feature tests written but requiring `--seed-rollover-deficit` infrastructure
- Phase 20: PredictiveActivityUITests created with 6 test stubs marked TODO
- Plan 21-01: UI polish completed (breakdown display, goal multiplier text)

**Codebase constraints:**
- iOS 17+ with Swift Testing for unit tests, XCUITest for E2E
- Launch arguments handled in JabTrackerApp.swift (lines 70-92)
- Test data seeding patterns in TestDataSeeding.swift
- Use `TestUtilities.launchAppWithTestMode()` pattern from CalorieExpenditureUITests
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create rollover seeding infrastructure</name>
  <files>
    JabTracker/Utils/TestDataSeeding.swift
    JabTracker/JabTrackerApp.swift
  </files>
  <action>
    Add rollover deficit/surplus seeding support:

    1. In TestDataSeeding.swift, add two new methods:
       - `seedRolloverDeficit(_ deficit: Int)` - Creates yesterday's food entries totaling `baseTarget - deficit` calories, enables `rolloverCaloriesEnabled` on user
       - `seedRolloverSurplus()` - Creates yesterday's food entries exceeding baseTarget, enables `rolloverCaloriesEnabled`

    2. In JabTrackerApp.swift, add launch argument handlers after line 82:
       - `--seed-rollover-deficit=X` - Calls `dataController.seedRolloverDeficit(X)`
       - `--seed-rollover-surplus` - Calls `dataController.seedRolloverSurplus()`

    Implementation notes:
    - Follow existing `seedCalorieExpenditureUser()` pattern
    - Use Calendar to calculate yesterday's date
    - Create a simple Food + FoodEntry with the target calories
    - Ensure `rolloverCaloriesEnabled = true` is set on the seeded user
    - Log seeding actions for debugging
  </action>
  <verify>
    Build succeeds: `./scripts/build.sh`
    Launch app with `--seed-rollover-deficit=200` and verify user is created with rollover enabled
  </verify>
  <done>
    Launch arguments `--seed-rollover-deficit=X` and `--seed-rollover-surplus` work and seed appropriate test data
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement RolloverCaloriesUITests</name>
  <files>
    JabTrackerUITests/RolloverCaloriesUITests.swift
  </files>
  <action>
    Complete the 5 feature behavior tests in RolloverCaloriesUITests.swift:

    1. `testRolloverAddsUnusedCalories()`:
       - Launch with `["--ui-testing", "--reset-app-data", "--seed-rollover-deficit=200"]`
       - Navigate to Dashboard tab
       - Verify calorie target shows 2200 (2000 base + 200 rollover)
       - Use `macro-remaining-calories` accessibility identifier

    2. `testRolloverCappedAt200()`:
       - Launch with `["--ui-testing", "--reset-app-data", "--seed-rollover-deficit=500"]`
       - Navigate to Dashboard
       - Verify calorie target shows 2200 (capped at 200, not 500)

    3. `testNoRolloverWhenOverBudget()`:
       - Launch with `["--ui-testing", "--reset-app-data", "--seed-rollover-surplus"]`
       - Navigate to Dashboard
       - Verify calorie target shows 2000 (no rollover)

    4. `testRolloverStacksWithBurnedCalories()`:
       - Launch with `["--ui-testing", "--reset-app-data", "--seed-rollover-deficit=150", "--mock-active-energy=250"]`
       - Enable burned calories toggle in settings
       - Navigate to Dashboard
       - Verify calorie target shows 2400 (2000 + 150 rollover + 250 burned)

    5. `testRolloverDisabledShowsBaseTarget()`:
       - Launch with seeded deficit
       - Disable rollover toggle in settings
       - Verify calorie target shows 2000 (base only)

    Test patterns to follow:
    - Use `TestUtilities.navigateToTab(app, tabName:)` for navigation
    - Use NSPredicate for flexible text matching: `label CONTAINS '2,200' OR label CONTAINS '2200'`
    - Add teardown screenshot capture on failure
  </action>
  <verify>
    Run tests: `./scripts/test.sh ui 1 RolloverCaloriesUITests`
    All 7 tests pass (2 existing toggle tests + 5 new feature tests)
  </verify>
  <done>
    RolloverCaloriesUITests has 7 passing tests covering toggle behavior and feature validation
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement PredictiveActivityUITests</name>
  <files>
    JabTrackerUITests/PredictiveActivityUITests.swift
  </files>
  <action>
    Implement the 6 test stubs in PredictiveActivityUITests.swift:

    1. `testTogglePredictiveActivityOn()`:
       - Launch with `["--ui-testing", "--reset-app-data", "--seed-calorie-user"]`
       - Navigate to More > Calorie Expenditure
       - Toggle predictive activity on
       - Verify toggle state changes

    2. `testPredictiveToggleDisabledWithoutHealthSync()`:
       - Launch WITHOUT `--seed-calorie-user` (healthSyncEnabled = false)
       - Navigate to Calorie Expenditure
       - Verify predictive toggle is disabled

    3. `testPredictiveActivityIncreasesTarget()`:
       - Launch with predictive enabled user + mock activity history
       - Note: May need to add `--seed-predictive-user` or use existing seeding + enable toggle
       - Verify target increases from base

    4. `testPredictiveAppliesWeightLossMultiplier()`:
       - Launch with weightLoss goal user
       - Verify predictive adjustment is 80% of activity average

    5. `testPredictiveAppliesMuscleGainMultiplier()`:
       - Launch with muscleGain goal user
       - Verify predictive adjustment is 120% of activity average

    6. `testDisablingPredictiveRemovesBonus()`:
       - Launch with predictive enabled and adjustment showing
       - Disable predictive toggle
       - Verify target returns to base

    **Important:** Predictive activity is mutually exclusive with burned calories. Tests should ensure `addBurnedCaloriesEnabled = false` when testing predictive.

    **Seeding consideration:** If 7-day activity history seeding is complex, tests can verify toggle behavior and basic functionality. Add `// TODO: Full predictive calculation E2E pending activity history seeding` comment for tests that need additional infrastructure.
  </action>
  <verify>
    Run tests: `./scripts/test.sh ui 1 PredictiveActivityUITests`
    All 6 tests pass or have documented limitations
  </verify>
  <done>
    PredictiveActivityUITests has 6 implemented tests covering toggle behavior and feature validation
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 21 complete:
    - Plan 21-01: CalorieAdjustmentBreakdownView showing burned/rollover/predictive contribution
    - Plan 21-01: Goal multiplier text in CalorieExpenditureView footer
    - Plan 21-01: 20-SMOKETEST.md documentation
    - Plan 21-02: Rollover seeding infrastructure
    - Plan 21-02: RolloverCaloriesUITests (7 tests)
    - Plan 21-02: PredictiveActivityUITests (6 tests)
  </what-built>
  <how-to-verify>
    1. Build and run app: Open in Xcode, run on simulator

    2. Test breakdown display:
       - Enable "Add Burned Calories" in More > Calorie Expenditure
       - Go to Food Log tab
       - Verify "Today's Calorie Adjustments" card appears showing:
         - Base Target: 2000 kcal (or your target)
         - Burned: +X kcal (with flame icon)
         - Today's Target: adjusted total
       - If you enabled rollover, verify rollover row appears

    3. Test goal multiplier text:
       - Go to More > Calorie Expenditure
       - Look at Predictive Activity section footer
       - Verify it shows your goal multiplier (e.g., "Currently: 80% of activity (weight loss)")

    4. Run full E2E test suite:
       ```bash
       ./scripts/test.sh ui 1 CalorieExpenditureUITests
       ./scripts/test.sh ui 1 RolloverCaloriesUITests
       ./scripts/test.sh ui 1 PredictiveActivityUITests
       ```
       - All tests should pass

    5. Visual check:
       - Breakdown card styling looks good (rounded corners, proper spacing)
       - Icons render correctly (flame, arrow, chart)
       - Text is readable and properly aligned
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 21, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `./scripts/build.sh` succeeds
- [ ] `./scripts/test.sh ui 1 RolloverCaloriesUITests` - All tests pass
- [ ] `./scripts/test.sh ui 1 PredictiveActivityUITests` - All tests pass
- [ ] `./scripts/test.sh ui 1 CalorieExpenditureUITests` - No regressions
- [ ] CalorieAdjustmentBreakdownView displays correctly
- [ ] Goal multiplier text shows in settings
- [ ] SwiftLint passes: `swiftlint`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- RolloverCaloriesUITests: 7 tests passing
- PredictiveActivityUITests: 6 tests passing
- No regressions in existing CalorieExpenditureUITests
- Phase 21 complete, milestone v0.4.0 ready for final review
</success_criteria>

<output>
After completion, create `.planning/phases/21-integration-polish/21-02-SUMMARY.md`:

# Phase 21 Plan 02: E2E Test Infrastructure & Final Verification Summary

**[Substantive one-liner]**

## Performance
- Duration: X min
- Tasks: 4
- Files modified: X

## Accomplishments
- [Key outcomes]

## Files Created/Modified
- `path/to/file` - Description

## Decisions Made
[Key decisions and rationale, or "None"]

## Issues Encountered
[Problems and resolutions, or "None"]

## Next Step
Phase 21 complete. Milestone v0.4.0 ready for final review and merge.
</output>
