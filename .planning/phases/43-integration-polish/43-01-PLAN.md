---
phase: 43-integration-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - JabTracker/Views/Dashboard/Widgets/EnergyBalanceHeroWidget.swift
  - JabTracker/ViewModels/EnergyBalanceHeroViewModel.swift
autonomous: true

must_haves:
  truths:
    - "Hero widget shows correct daily average when fewer than 30 days of data exist"
    - "Hero widget daily average matches detail view calculation"
    - "Labels reflect actual data range (not always '30 Days')"
  artifacts:
    - path: "JabTracker/Views/Dashboard/Widgets/EnergyBalanceHeroWidget.swift"
      provides: "Fixed average calculation using actual day count"
      contains: "dailyCalories.count"
    - path: "JabTracker/ViewModels/EnergyBalanceHeroViewModel.swift"
      provides: "Day count accessor for widget"
      exports: ["dayCount"]
  key_links:
    - from: "EnergyBalanceHeroWidget.swift"
      to: "EnergyBalanceHeroViewModel"
      via: "dayCount property"
      pattern: "viewModel.*dayCount"
---

<objective>
Fix energy balance hero widget to calculate daily averages correctly when fewer than 30 days of data exist.

Purpose: User reports hero shows wrong values with ~4 days of data. The widget divides totalNutrition by hardcoded 30 instead of actual day count, producing incorrect daily averages. Detail view calculates correctly.

Output: Hero widget shows accurate daily averages matching detail view behavior.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@JabTracker/Views/Dashboard/Widgets/EnergyBalanceHeroWidget.swift
@JabTracker/ViewModels/EnergyBalanceHeroViewModel.swift
@JabTracker/ViewModels/EnergyBalanceWidgetViewModel.swift (reference - correct pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dayCount accessor to ViewModel</name>
  <files>JabTracker/ViewModels/EnergyBalanceHeroViewModel.swift</files>
  <action>
Add a computed property to expose the actual number of days with data:

```swift
/// Number of days with meaningful data (for average calculations)
var dayCount: Int {
    dailyCalories.count
}
```

This follows the same pattern as EnergyBalanceWidgetViewModel which uses balances.count for its averages.
  </action>
  <verify>Build succeeds with `./scripts/build.sh`</verify>
  <done>ViewModel exposes dayCount property returning dailyCalories.count</done>
</task>

<task type="auto">
  <name>Task 2: Fix hero widget average calculation</name>
  <files>JabTracker/Views/Dashboard/Widgets/EnergyBalanceHeroWidget.swift</files>
  <action>
Fix the hardcoded division in `summaryEquationRow` (around line 226):

**Current (buggy):**
```swift
let avgNutrition = totalNutrition / 30
```

**Fixed:**
```swift
let dayCount = useMockData ? dailyCalories.count : (viewModel?.dayCount ?? 1)
let avgNutrition = dayCount > 0 ? totalNutrition / dayCount : 0
```

Also update the "Last 30 Days" label (around line 92) to reflect actual data:
```swift
Text(dayCount == 30 ? "Last 30 Days" : "Last \(dayCount) Days")
```

Add a private accessor similar to other data accessors:
```swift
private var dayCount: Int {
    useMockData ? dailyCalories.count : (viewModel?.dayCount ?? 0)
}
```

Then use this throughout for consistency.
  </action>
  <verify>Build succeeds with `./scripts/build.sh`</verify>
  <done>Hero widget calculates avgNutrition using actual dayCount instead of hardcoded 30</done>
</task>

<task type="auto">
  <name>Task 3: Run lint and verify no regressions</name>
  <files>JabTracker/ViewModels/EnergyBalanceHeroViewModel.swift, JabTracker/Views/Dashboard/Widgets/EnergyBalanceHeroWidget.swift</files>
  <action>
Run SwiftLint to ensure no new violations:
```bash
swiftlint
```

Verify the mock data still works correctly - the mock generates 30 days so the label should still show "Last 30 Days" in previews.
  </action>
  <verify>`swiftlint` passes with no new errors</verify>
  <done>Code passes lint checks, mock data behavior preserved</done>
</task>

</tasks>

<verification>
1. Build succeeds: `./scripts/build.sh`
2. Lint passes: `swiftlint`
3. Mock data still shows "Last 30 Days" (30 days generated)
4. Real data with <30 days shows correct label and averages
</verification>

<success_criteria>
- [ ] avgNutrition calculated using dailyCalories.count, not hardcoded 30
- [ ] "Last N Days" label reflects actual data range
- [ ] Build passes
- [ ] Lint passes
</success_criteria>

<output>
After completion, create `.planning/phases/43-integration-polish/43-01-SUMMARY.md`
</output>
