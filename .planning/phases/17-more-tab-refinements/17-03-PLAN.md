---
phase: 17-more-tab-refinements
plan: 03
type: execute
---

<objective>
Create CalorieExpenditureView mock screen, add inactive placeholders, and create E2E test stubs.

Purpose: Complete the More tab polish with mock screens for future features and establish E2E test foundation.
Output: CalorieExpenditureView.swift (mock), updated MoreView placeholders, E2E test stubs
</objective>

<execution_context>
~/.claude/gsd/workflows/execute-phase.md
~/.claude/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-more-tab-refinements/17-CONTEXT.md

**Codebase constraints:**
- Follow DesignCard patterns from SettingsView.swift
- Use DesignTokens for typography and colors
- Match mock screen pattern from SubscriptionView (created in 17-01)
- E2E tests use XCUITest with accessibility identifiers

**Key source files:**
@JabTracker/Views/Settings/SettingsView.swift
@JabTracker/Views/More/MoreView.swift (updated in 17-01)
@JabTrackerUITests/Utils/TestUtilities.swift
@JabTrackerUITests/SettingsUITests.swift

**Prior decisions affecting this phase:**
- Inactive items shown as gray Label without NavigationLink (17-01)
- SubscriptionView mock pattern established in 17-01
- All interactive elements need accessibility identifiers
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CalorieExpenditureView mock screen</name>
  <files>JabTracker/Views/Settings/CalorieExpenditureView.swift</files>
  <action>
    Create CalorieExpenditureView.swift as a mock/placeholder screen:

    1. Use ScrollView with VStack(spacing: 24) pattern from SettingsView

    2. Add "Coming Soon" banner at top:
       ```swift
       Text("Coming Soon")
           .font(DesignTokens.Typography.caption)
           .foregroundColor(.secondary)
           .padding(.top, 8)
       ```

    3. "Daily Calorie Budget" DesignCard:
       - Header: "Daily Calorie Budget"
       - Show mock value: "2,400 calories"
       - Description: "Your target daily calorie intake based on your goals"
       - All text should be static/mock data

    4. "Activity Level" DesignCard:
       - Header: "Activity Level"
       - Show mock selector with options (grayed out):
         - Sedentary
         - Lightly Active
         - Moderately Active (selected mock)
         - Very Active
         - Extremely Active
       - Footer: "Affects your daily calorie calculation"

    5. "TDEE Breakdown" DesignCard:
       - Header: "Total Daily Energy Expenditure"
       - Show mock breakdown:
         - BMR: 1,800 cal
         - Activity: 400 cal
         - TEF: 200 cal
         - Total: 2,400 cal
       - Footer: "Based on your profile and activity level"

    6. Add small gray "This feature is coming soon" text at bottom

    Required imports: SwiftUI
    Use DesignCard and DesignTokens patterns

    Add accessibility identifiers:
    - "calorie-expenditure-view"
    - "daily-budget-card"
    - "activity-level-card"
    - "tdee-breakdown-card"
  </action>
  <verify>Build succeeds, CalorieExpenditureView displays mock content with proper styling</verify>
  <done>CalorieExpenditureView created as mock screen with calorie budget, activity level, and TDEE breakdown sections</done>
</task>

<task type="auto">
  <name>Task 2: Wire CalorieExpenditureView and verify inactive placeholders</name>
  <files>JabTracker/Views/More/MoreView.swift</files>
  <action>
    Update MoreView.swift to wire CalorieExpenditureView and ensure all inactive items:

    1. Add NavigationLink for CalorieExpenditureView:
       ```swift
       NavigationLink(destination: CalorieExpenditureView()) {
           Label("Calorie Expenditure", systemImage: "flame")
       }
       ```

    2. Verify these items are gray Label only (no NavigationLink):
       - "Food Log" in Feature Settings section
       - "Shortcuts & Tabs" in Feature Settings section
       - "FAQ" in Support section
       - "Help & Support" in Support section

    3. Each inactive item should look like:
       ```swift
       Label("Food Log", systemImage: "fork.knife")
           .foregroundColor(.secondary)
       ```

    4. Add accessibility identifiers to inactive items:
       - "food-log-placeholder"
       - "shortcuts-placeholder"
       - "faq-placeholder"
       - "help-support-placeholder"
  </action>
  <verify>Build succeeds, CalorieExpenditureView accessible from MoreView, inactive items appear gray</verify>
  <done>CalorieExpenditureView wired to MoreView, all inactive placeholders verified with correct styling</done>
</task>

<task type="auto">
  <name>Task 3: Create E2E test stubs for More tab flows</name>
  <files>JabTrackerUITests/MoreTabUITests.swift</files>
  <action>
    Create MoreTabUITests.swift with E2E test stubs:

    ```swift
    import XCTest

    final class MoreTabUITests: XCTestCase {
        var app: XCUIApplication!

        override func setUpWithError() throws {
            continueAfterFailure = false
            app = TestUtilities.launchAppWithTestMode(resetData: true, seedData: "7d")
        }

        // MARK: - Navigation Tests

        func testNavigateToSecurityPrivacy() throws {
            TestUtilities.navigateToTab(app, tabName: "More")

            let moreView = app.otherElements["more-view"]
            XCTAssertTrue(moreView.waitForExistence(timeout: 5))

            app.cells["security-privacy-row"].tap()

            let securityView = app.otherElements["security-privacy-view"]
            XCTAssertTrue(securityView.waitForExistence(timeout: 5))
        }

        func testNavigateToSubscription() throws {
            TestUtilities.navigateToTab(app, tabName: "More")

            app.cells["subscription-row"].tap()

            let subscriptionView = app.otherElements["subscription-view"]
            XCTAssertTrue(subscriptionView.waitForExistence(timeout: 5))
        }

        func testNavigateToNotificationSettings() throws {
            TestUtilities.navigateToTab(app, tabName: "More")

            app.cells["notifications-row"].tap()

            let notificationView = app.otherElements["notification-settings-view"]
            XCTAssertTrue(notificationView.waitForExistence(timeout: 5))
        }

        func testNavigateToCalorieExpenditure() throws {
            TestUtilities.navigateToTab(app, tabName: "More")

            app.cells["calorie-expenditure-row"].tap()

            let expenditureView = app.otherElements["calorie-expenditure-view"]
            XCTAssertTrue(expenditureView.waitForExistence(timeout: 5))
        }

        // MARK: - Overflow Menu Tests

        func testNavigateToGoalsStrategy() throws {
            TestUtilities.navigateToTab(app, tabName: "More")

            app.cells["goals-strategy-row"].tap()

            let strategyView = app.otherElements["strategy-view"]
            XCTAssertTrue(strategyView.waitForExistence(timeout: 5))
        }

        func testNavigateToFoodLibrary() throws {
            TestUtilities.navigateToTab(app, tabName: "More")

            app.cells["food-library-row"].tap()

            let libraryView = app.otherElements["food-library-view"]
            XCTAssertTrue(libraryView.waitForExistence(timeout: 5))
        }

        // MARK: - Inactive Placeholder Tests

        func testInactiveItemsNotTappable() throws {
            TestUtilities.navigateToTab(app, tabName: "More")

            // These should exist but not navigate
            let foodLogPlaceholder = app.staticTexts["food-log-placeholder"]
            XCTAssertTrue(foodLogPlaceholder.exists)

            let shortcutsPlaceholder = app.staticTexts["shortcuts-placeholder"]
            XCTAssertTrue(shortcutsPlaceholder.exists)

            let faqPlaceholder = app.staticTexts["faq-placeholder"]
            XCTAssertTrue(faqPlaceholder.exists)
        }

        // MARK: - Security & Privacy Tests

        func testBiometricToggleExists() throws {
            TestUtilities.navigateToTab(app, tabName: "More")
            app.cells["security-privacy-row"].tap()

            let biometricToggle = app.switches["biometric-toggle"]
            // May not exist on simulator without biometric support
            // Just verify view loads
            let securityView = app.otherElements["security-privacy-view"]
            XCTAssertTrue(securityView.waitForExistence(timeout: 5))
        }

        func testHealthToggleExists() throws {
            TestUtilities.navigateToTab(app, tabName: "More")
            app.cells["security-privacy-row"].tap()

            let healthToggle = app.switches["health-toggle"]
            XCTAssertTrue(healthToggle.waitForExistence(timeout: 5))
        }

        // MARK: - Notification Settings Tests

        func testNotificationTogglesExist() throws {
            TestUtilities.navigateToTab(app, tabName: "More")
            app.cells["notifications-row"].tap()

            let notificationView = app.otherElements["notification-settings-view"]
            XCTAssertTrue(notificationView.waitForExistence(timeout: 5))

            // Verify key toggles exist
            XCTAssertTrue(app.switches["weigh-in-daily-toggle"].exists)
            XCTAssertTrue(app.switches["dose-reminder-toggle"].exists)
        }
    }
    ```

    Add accessibility identifiers to MoreView rows if not already present:
    - "security-privacy-row"
    - "subscription-row"
    - "notifications-row"
    - "calorie-expenditure-row"
    - "goals-strategy-row"
    - "food-library-row"
    - "medications-row"
  </action>
  <verify>Build succeeds, E2E tests compile (may not all pass until full implementation complete)</verify>
  <done>E2E test stubs created for More tab navigation and settings flows</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./scripts/build.sh` succeeds without errors
- [ ] `swiftlint` passes (or only pre-existing warnings)
- [ ] CalorieExpenditureView accessible from MoreView
- [ ] CalorieExpenditureView displays mock content correctly
- [ ] Inactive placeholders appear gray and are not tappable
- [ ] E2E test file compiles
- [ ] xcodegen generate runs successfully after new file creation
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No new SwiftLint errors introduced
- CalorieExpenditureView matches mock screen pattern from SubscriptionView
- All inactive items properly styled as placeholders
- E2E test stubs ready for expansion
</success_criteria>

<output>
After completion, create `.planning/phases/17-more-tab-refinements/17-03-SUMMARY.md`:

# Phase 17 Plan 03: Mock Screens & Polish Summary

**[Substantive one-liner about what was accomplished]**

## Accomplishments
- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified
- `path/to/file.swift` - Description

## Decisions Made
[Key decisions and rationale, or "None"]

## Issues Encountered
[Problems and resolutions, or "None"]

## Next Step
Phase 17 complete - ready for milestone completion or next phase
</output>
