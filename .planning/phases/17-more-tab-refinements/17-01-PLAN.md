---
phase: 17-more-tab-refinements
plan: 01
type: execute
---

<objective>
Restructure MoreView with cleaner organization and create core settings screens.

Purpose: Reorganize the More tab with intuitive groupings and add Security & Privacy and Subscription screens.
Output: Refactored MoreView.swift, new SecurityPrivacyView.swift, new SubscriptionView.swift (mock)
</objective>

<execution_context>
~/.claude/gsd/workflows/execute-phase.md
~/.claude/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-more-tab-refinements/17-CONTEXT.md

**Codebase constraints:**
- Follow existing List + Section pattern from AccountView.swift
- Use DesignTokens for typography and colors
- Add accessibility identifiers to all interactive elements
- Use .insetGrouped list style

**Key source files:**
@JabTracker/Views/More/MoreView.swift
@JabTracker/Views/Settings/AccountView.swift
@JabTracker/Views/Settings/SettingsView.swift
@JabTracker/Design/DesignTokens.swift

**Prior decisions affecting this phase:**
- Phase 17 evolved from "Goal Settings Integration" to "More Tab Refinements" since goal editing was completed in phases 15.2 and 16
- User Card removal requested per 17-CONTEXT.md
- Inactive items should be gray text as placeholders
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure MoreView with new sections</name>
  <files>JabTracker/Views/More/MoreView.swift</files>
  <action>
    Refactor MoreView.swift to match the new structure from 17-CONTEXT.md:

    1. REMOVE the userHeaderSection entirely (no User Card)

    2. Add new section at top - "Overflow Menu" (items not in top-level tabs):
       - "Goals & Strategy" → NavigationLink to StrategyView (icon: target)
       - "Food Library" → NavigationLink to FoodLibraryView (icon: book.closed)
       - "GLP-1 Medications" → NavigationLink to MedicationProfileSettingsView (icon: pills)

    3. Rename "Feature Settings" section and update items:
       - "Dashboard" → BodyMetricsVisibilityView (icon: square.grid.2x2) - keep existing
       - "Food Log" → gray/disabled placeholder (icon: fork.knife)
       - "Metrics" → BodyMetricsVisibilityView (icon: chart.bar) - rename from Dashboard
       - "Units of Measurement" → UnitsOfMeasureView (icon: ruler)
       - "Calorie Expenditure" → CalorieExpenditureView (icon: flame) - will be created in 17-03
       - "Shortcuts & Tabs" → gray/disabled placeholder (icon: bolt.horizontal)

    4. Add "Account Settings" section:
       - "Profile" → AccountView (icon: person.circle) - renamed from Account
       - "Subscription" → SubscriptionView (icon: tag) - will create in this plan
       - "Security & Privacy" → SecurityPrivacyView (icon: lock.shield) - will create in this plan
       - "Notifications" → NotificationSettingsView (icon: bell) - will be created in 17-02

    5. Add "Support" section:
       - "FAQ" → gray/disabled placeholder (icon: questionmark.circle)
       - "Help & Support" → gray/disabled placeholder (icon: lifepreserver)
       - "General" → keep link to existing GeneralSettingsView or create simple about view (icon: info.circle)

    For disabled/inactive items, use:
    ```swift
    Label("Food Log", systemImage: "fork.knife")
        .foregroundColor(.secondary)
    ```
    (No NavigationLink wrapper, just the Label with gray color)

    Keep .listStyle(.insetGrouped), .navigationTitle("More"), .navigationBarTitleDisplayMode(.large)
  </action>
  <verify>Build succeeds, MoreView displays all sections with correct navigation</verify>
  <done>MoreView restructured with overflow menu, feature settings, account settings, and support sections. User Card removed. Inactive items shown in gray.</done>
</task>

<task type="auto">
  <name>Task 2: Create SecurityPrivacyView</name>
  <files>JabTracker/Views/Settings/SecurityPrivacyView.swift</files>
  <action>
    Create SecurityPrivacyView.swift following AccountView patterns:

    1. Use List with .insetGrouped style

    2. "Security" section:
       - Face ID/Touch ID toggle row (copy pattern from AccountView lines 113-135)
       - Use biometricManager.isAvailable to conditionally show
       - Icon: faceid or touchid based on biometricManager.biometricType
       - Toggle bound to $biometricManager.isBiometricEnabled
       - Footer: "Use [Face ID/Touch ID] to protect your data and require authentication when opening the app."

    3. "Integrations" section:
       - Health toggle row (copy pattern from AccountView lines 138-155)
       - Icon: heart.fill with .red color
       - Toggle bound to healthToggleBinding (same pattern as AccountView)
       - Footer: "Sync weight, height, and biometrics from the Health app to keep your profile up to date."

    4. "iCloud Sync" section:
       - Display sync status using SyncStatusCard pattern from SettingsView
       - Show icon, status text, and action button if needed
       - Use dataController.syncStatus and dataController.syncStatusMessage

    Required imports: SwiftUI, SwiftData, OSLog
    Required environment objects: BiometricAuthManager, AuthenticationManager
    Required @Query: [User]
    Required @ObservedObject: DataController.shared

    Add accessibility identifiers:
    - "security-privacy-view"
    - "biometric-toggle"
    - "health-toggle"
    - "sync-status-section"
  </action>
  <verify>Build succeeds, SecurityPrivacyView displays all three sections with working toggles</verify>
  <done>SecurityPrivacyView created with Face ID toggle, Health toggle, and iCloud sync status display</done>
</task>

<task type="auto">
  <name>Task 3: Create SubscriptionView (mock)</name>
  <files>JabTracker/Views/Settings/SubscriptionView.swift</files>
  <action>
    Create SubscriptionView.swift as a mock/placeholder screen:

    1. Use ScrollView with VStack(spacing: 24) pattern from SettingsView

    2. Add "Coming Soon" banner at top:
       ```swift
       Text("Coming Soon")
           .font(DesignTokens.Typography.caption)
           .foregroundColor(.secondary)
           .padding(.top, 8)
       ```

    3. "Active Subscription" DesignCard:
       - Plan name: "12 Months"
       - Price: "$59.99 (n% off monthly)"
       - Renewal text: "Renews on May 8, 2026"
       - All text should be static/mock data

    4. "Restore Purchases" DesignCard:
       - Button "Restore Purchases" (disabled, shows as placeholder)
       - Style: .bordered, gray color

    5. "Manage Subscription" DesignCard:
       - Description text explaining App Store management
       - Button "Manage Subscription" that opens App Store subscription URL
       - Footer text about auto-renewal

    6. Add small gray "This is a preview" text at bottom

    Required imports: SwiftUI
    Use DesignCard and DesignTokens patterns from SettingsView

    Add accessibility identifiers:
    - "subscription-view"
    - "subscription-plan-card"
    - "restore-purchases-button"
    - "manage-subscription-button"
  </action>
  <verify>Build succeeds, SubscriptionView displays mock subscription info with proper styling</verify>
  <done>SubscriptionView created as mock screen with plan details, restore purchases, and manage subscription sections</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./scripts/build.sh` succeeds without errors
- [ ] `swiftlint` passes (or only pre-existing warnings)
- [ ] MoreView shows new structure with all sections
- [ ] Navigation to SecurityPrivacyView works
- [ ] Navigation to SubscriptionView works
- [ ] Face ID toggle in SecurityPrivacyView is functional
- [ ] Health toggle in SecurityPrivacyView is functional
- [ ] Inactive items appear in gray
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No new SwiftLint errors introduced
- MoreView restructured per 17-CONTEXT.md specification
- SecurityPrivacyView functional with working toggles
- SubscriptionView displays mock content correctly
</success_criteria>

<output>
After completion, create `.planning/phases/17-more-tab-refinements/17-01-SUMMARY.md`:

# Phase 17 Plan 01: Core Restructure Summary

**[Substantive one-liner about what was accomplished]**

## Accomplishments
- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified
- `path/to/file.swift` - Description

## Decisions Made
[Key decisions and rationale, or "None"]

## Issues Encountered
[Problems and resolutions, or "None"]

## Next Step
Ready for 17-02-PLAN.md (Notification Settings)
</output>
