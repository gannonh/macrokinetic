---
phase: 04-barcode-assignment
plan: 01
type: execute
---

<objective>
Camera service and scanner UI integrated into FoodSearchSheet when Scan tab selected.

Purpose: Enable camera-based barcode scanning UI within the existing food search flow.
Output: Working camera preview with Barcode/Label sub-toggle when user selects Scan tab.
</objective>

<execution_context>
~/.claude/gsd/workflows/execute-phase.md
~/.claude/gsd/templates/summary.md
~/.claude/skills/ios-dev/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md

**Prior decisions affecting this phase:**
- Phase 1: Reuse Food model with source = .userCreated (barcode field exists)
- Phase 1: Barcode uniqueness within custom foods only

**Codebase constraints:**
- iOS 17+ only (can use modern APIs)
- Zero external dependencies (AVFoundation is Apple framework)
- MVVM with @Observable pattern
- Services follow NotificationService permission pattern

**Key files:**
@JabTracker/Views/Nutrition/FoodSearchSheet.swift
@JabTracker/Views/Nutrition/SearchMethodTabs.swift
@JabTracker/Services/NotificationService.swift (permission pattern reference)
@JabTracker/Info.plist

**Target UI (from reference screenshots):**
- When Scan tab selected, replace search field + content with scanner view
- Sub-toggle row: "Barcode | Label" pills (Label disabled initially)
- Right side of sub-toggle: camera gallery icon + torch icon
- Below: Full camera preview with scanning reticle
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CameraService with permission handling</name>
  <files>JabTracker/Services/CameraService.swift, JabTracker/Info.plist</files>
  <action>
    **Create CameraService.swift:**
    - `@Observable @MainActor final class CameraService`
    - Properties: `authorizationStatus: AVAuthorizationStatus`, `session: AVCaptureSession?`, `isSessionRunning: Bool`
    - `requestAuthorization() async -> Bool` - Request camera permission
    - `checkAuthorizationStatus() -> AVAuthorizationStatus` - Check current status
    - `startSession() throws` - Configure and start AVCaptureSession with video input
    - `stopSession()` - Stop and clean up session
    - Follow NotificationService pattern for permission handling
    - Use OSLog logger with category "CameraService"
    - Barcode metadata detection will be added in Plan 2 (keep session simple for now)

    **Update Info.plist:**
    - Add `NSCameraUsageDescription` key
    - Value: "MacroKinetic uses the camera to scan food barcodes for quick nutrition lookup."

    **Important:**
    - Do NOT add to AppServices yet (will be owned by BarcodeScannerContentView)
    - Session runs on background queue, UI updates on main actor
  </action>
  <verify>./scripts/build.sh succeeds, CameraService compiles without errors</verify>
  <done>CameraService with permission request and basic session management, Info.plist has camera permission</done>
</task>

<task type="auto">
  <name>Task 2: Create BarcodeScannerContentView with camera preview</name>
  <files>JabTracker/Views/Nutrition/BarcodeScannerContentView.swift, JabTracker/Views/Nutrition/CameraPreviewView.swift</files>
  <action>
    **Create CameraPreviewView.swift:**
    - UIViewRepresentable wrapping AVCaptureVideoPreviewLayer
    - Init with `AVCaptureSession`
    - Configure videoGravity as .resizeAspectFill
    - Handle lifecycle (appear/disappear)

    **Create BarcodeScannerContentView.swift:**
    - `struct BarcodeScannerContentView: View`
    - `@State private var cameraService = CameraService()`
    - `@State private var selectedScanType: ScanType = .barcode` (enum: .barcode, .label)
    - `@State private var isTorchOn = false`
    - `let onBarcodeDetected: (String) -> Void` (placeholder for Plan 2)

    **UI Layout (VStack):**
    1. Sub-toggle row (HStack):
       - "Barcode" pill (selected by default)
       - "Label" pill (disabled, grayed out)
       - Spacer
       - Gallery button (camera.fill.badge.ellipsis icon, disabled for now)
       - Torch button (bolt.fill icon, toggles torch)

    2. Camera preview (fills remaining space):
       - ZStack with CameraPreviewView
       - Scanning reticle overlay (rounded rectangle outline, white with opacity)
       - Instruction text below reticle: "Point at a barcode"

    3. Permission denied state:
       - If authorization denied, show ContentUnavailableView
       - "Camera Access Required" with Settings button

    **Lifecycle:**
    - `.onAppear`: Check permission, request if needed, start session if authorized
    - `.onDisappear`: Stop session
    - Torch toggle: `AVCaptureDevice.default(for: .video)?.torchMode`

    **Accessibility:**
    - `barcode-scanner-content` identifier
    - `scan-type-barcode` and `scan-type-label` for pills
    - `torch-toggle-button` identifier
  </action>
  <verify>./scripts/build.sh succeeds, BarcodeScannerContentView renders in preview (may need simulator for camera)</verify>
  <done>BarcodeScannerContentView with camera preview, sub-toggle row, torch button, and permission handling</done>
</task>

<task type="auto">
  <name>Task 3: Integrate scanner into FoodSearchSheet when Scan tab selected</name>
  <files>JabTracker/Views/Nutrition/FoodSearchSheet.swift, JabTracker/Views/Nutrition/SearchMethodTabs.swift</files>
  <action>
    **Update SearchMethodTabs.swift:**
    - Line 44-47: Change `.scan` case to `return true` (enable the tab)

    **Update FoodSearchSheet.swift:**
    - Add state: `@State private var scannedBarcode: String?`

    - Modify `body`:
      - When `viewModel.selectedMethod == .scan`, hide `searchFieldSection`
      - Show `BarcodeScannerContentView` instead of `contentSection`

    - Update layout structure:
      ```swift
      VStack(spacing: 0) {
          headerSection
          methodTabsSection

          if viewModel.selectedMethod == .scan {
              BarcodeScannerContentView { barcode in
                  scannedBarcode = barcode
                  // Lookup handling in Plan 2
              }
          } else {
              searchFieldSection
              contentSection
          }
      }
      ```

    **Note:** The onBarcodeDetected callback is a placeholder - actual detection logic will be wired in Plan 2. For now, camera preview displays but doesn't detect barcodes yet.
  </action>
  <verify>./scripts/build.sh succeeds, Scan tab now enabled and shows camera preview when selected</verify>
  <done>FoodSearchSheet shows camera scanner when Scan tab selected, tab is enabled</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./scripts/build.sh` succeeds without errors
- [ ] Scan tab is enabled in FoodSearchSheet method tabs
- [ ] Selecting Scan tab shows camera preview (or permission request)
- [ ] Torch button toggles flash
- [ ] Barcode/Label sub-toggle visible (Label disabled)
- [ ] Switching back to Search tab shows search UI
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Camera preview renders when Scan tab selected
- Permission flow works (request → authorized or denied states)
- No build warnings or errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/04-barcode-assignment/04-01-SUMMARY.md`

# Phase 4 Plan 1: Camera Scanner UI Summary

**[One-liner summary of what shipped]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `path/to/file.swift` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 04-02-PLAN.md (Barcode Detection + Lookup Integration)
</output>
