# Milestone v0.8.0: Food Search & Library

**Status:** ✅ SHIPPED 2026-01-13
**Phases:** 35-38
**Total Plans:** 4

## Overview

Dramatically improved food search UX with better performance, ranking, serving size handling, and local-only barcode lookup.

## Phases

### Phase 35: Search Performance & UX

**Goal**: Fix sluggish typing, auto-focus input, expand amount field tap target, polish search header
**Depends on**: Previous milestone complete
**Plans**: 1 plan

Plans:
- [x] 35-01: Search UX fixes (debounce, auto-focus, tap targets)

**Details:**
- Fixed search debouncing with proper task cancellation pattern
- Auto-focus search field on sheet appearance using @FocusState
- Expanded amount input tap target with contentShape(Rectangle())
- Task cancellation pattern: cancel previous task, check Task.isCancelled after sleep

### Phase 35.1: Food Search Header Indicators (INSERTED)

**Goal**: Update Food Search sheet header to show kcal and protein remaining indicators matching the Food Log view style (progress bars with "X left" labels)
**Depends on**: Phase 35
**Plans**: 1 plan

Plans:
- [x] 35.1-01: Food Search header indicators

**Details:**
- Added consumedCalories, targetCalories, consumedProtein, targetProtein to FoodSearchSheetViewModel
- Replaced text-based indicators with MacroProgressBar components
- Visual consistency between Food Log and Food Search views

### Phase 36: Search Ranking & Recall (SKIPPED)

**Goal**: Improve FTS5 ranking so common foods surface easily (apples, eggs, etc.)
**Depends on**: Phase 35
**Status**: Skipped - to be revisited in future milestone

**Notes:**
Some ranking improvements were made (BM25, USDA boosting, whole word matching) but full phase scope deferred.

### Phase 37: Unit/Serving Strategy

**Goal**: Research competitors and implement serving size improvements (1 large egg, 1 medium apple)
**Depends on**: Phase 36
**Plans**: 1 plan

Plans:
- [x] 37-01: Horizontal pill picker for serving unit selection

**Details:**
- Created ServingPillPicker component with horizontal scrollable pills
- Auto-parses multi-word descriptions from database (e.g., "whole without shell")
- Always includes g/oz as universal fallback options
- Smart serving count conversion when switching between pills
- Item-based options reset to 1, unit-only options preserve gram amount

### Phase 38: Bug Fixes & Cleanup

**Goal**: Fix barcode scanner bug, remove API fallback
**Depends on**: Phase 37
**Plans**: 1 plan

Plans:
- [x] 38-01: Fix barcode scanner, remove Open Food Facts API, fix ISS-001

**Details:**
- Fixed barcode lookup to query local database (1.7M+ foods) instead of external API
- Removed entire OpenFoodFactsService - all food data now local-only
- Deleted dead code: FoodSearchView.swift, AddFoodSheet.swift, MealLogView.swift
- Fixed ShortcutButton tap target to cover entire button area (ISS-001)

---

## Milestone Summary

**Decimal Phases:**
- Phase 35.1: Food Search Header Indicators (inserted after Phase 35 for UX consistency)

**Key Decisions:**
- Removed ALL Open Food Facts API code - local database (1.7M foods) is sufficient
- If barcode not found locally, user creates custom food (no API fallback)
- Horizontal pill picker for serving unit selection (MacroFactor-inspired UX)
- MacroProgressBar for consistent macro tracking display across views

**Issues Resolved:**
- ISS-001: ShortcutButton tap target now covers entire button area

**Issues Deferred:**
- Phase 36 (Search Ranking & Recall) - partial implementation, full scope to future milestone

**Technical Debt Incurred:**
- None

---

_For current project status, see .planning/ROADMAP.md_
