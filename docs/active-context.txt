  🧪 Ready for Your Smoke Testing

  Please test the following workflows:

  1. Calendar Display (Stream A)

  - Open History tab → Calendar view
  - Verify scheduled dose indicators appear
  - Check color coding (blue outline = scheduled, blue filled = logged, red = missed)
  - Test month navigation

  2. Dose Actions (Stream B)

  - Long-press on day with scheduled dose
  - Verify action sheet appears
  - Test "Log Dose" → QuickDoseSheet pre-population
  - Test "Reschedule" → date picker + smart suggestions
  - Test "Skip" → dose removed from calendar

  3. Statistics Display (Stream C)

  - Check MonthlyStatsView for schedule adherence section
  - Verify adherence rate percentage displays
  - Check breakdown: "X taken / Y scheduled (Z missed, W skipped)"

  ---
  📝 After Smoke Testing

  Once you confirm the UI/UX works correctly:

  1. I'll implement the 27 E2E test stubs using debug-first approach
  2. Run full test suite to validate integration
  3. Code review and final pol