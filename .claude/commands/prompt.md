1. In JabTracker/Views/DoseEntry/TitrationConfirmationDialog.swift around line 31, remove the unused @State private var rescheduleDate: Date = Date() declaration; delete that single unused state property and ensure there are no remaining references to rescheduleDate elsewhere in the file (if any, replace or remove them) so the view compiles without unused-state warnings.
2. In JabTracker/Views/DoseEntry/TitrationConfirmationDialog.swift around lines 215 to 237, the reschedule sheet stays open when saving fails and hides the underlying dialog error; before attempting the save, set showingReschedulePicker = false so the sheet is dismissed regardless of the save outcome, then proceed with setting isProcessing, updating titration, calling try modelContext.save(), and in the catch keep setting errorMessage and isProcessing = false to keep UI state consistent.