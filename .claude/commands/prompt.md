Learnings:

- Tests validate expected behavior; We write acceptance criteria first because this is the spec; then we implement and wire up the actual test methods. If expected elements are missing, incomplete or wrong, this means the implementation needs work. IT DOES NOT MEAN THAT WE SHOULD WEAKEN THE TEST TO PASS
- If having trouble finding locators, even after using the debug utils, ask the user to do a codegen recording and share the results.