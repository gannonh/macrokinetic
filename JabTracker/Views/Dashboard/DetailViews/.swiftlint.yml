# SwiftLint config for DetailViews directory
# Detail views have larger mock data structures and multi-section layouts
# Relaxed thresholds for file and type body length

type_body_length:
  warning: 600
  error: 650

file_length:
  warning: 900
  error: 1000
